# Project Log — Nepal Data Portal

Running diary of every completed step. Updated at the end of every session.
Format: newest entry at the top.

---

| Date | Step | What was done | Evidence | Next |
|------|------|---------------|----------|------|
| 2026-08-07 | **ODN.S2 (data) — a decade of daily food prices loaded; the database caught a double-count I missed** | Kalimati wholesale prices in the warehouse: the daily LOW and daily HIGH for a 25-commodity basket, 2013-06-16 → 2022-04-18. Decisions, all recorded in code: **(1) the source is the market board, not Open Data Nepal** — crediting the aggregator would break the trail back to the office that can answer questions about the numbers; its type is `ministry`, the closest the schema allows, with a note saying plainly that a market development board is not a ministry. **(2) Geography is Kathmandu Metropolitan City, not Nepal** — these are one market's wholesale prices, and filing them nationally would assert they are Nepal's prices, which the source never claims; that Kalimati is the national benchmark market is a thing to say in words on the page, not to encode as a geography. **(3) The source's "Average" column is not stored** — it is exactly (min+max)/2 across the whole series and disagrees with the market board's own published average, so it is derived for display and labelled a midpoint. That also saved a third of the storage. **(4) Rows priced per piece or per dozen (6,910) are excluded and counted, never converted** — converting needs a weight the source does not give. Migration 0007 widens the `period_type` CHECK to allow `day`; day rows are created only for dates the data has, not pre-seeded. Basket and indicators are reviewable CSVs, the basket carrying each commodity's day-count so a human can argue with the selection. **The first load failed after 135,000 rows** on the `observations_unique_per_release` constraint: the two source resources overlap by ~4 months and the loader was offering the same commodity-day twice. The constraint was right — the alternative is a silently double-counted price. I had known about that overlap (I used it in S1 to verify the column mapping) and still did not de-duplicate; the database caught what I missed. | **153,494 observations** (76,747 commodity-days × low and high), 25 commodities, 3,091 trading days. **Duplicate latest cells: 0. Rows where the low exceeds the high: 0.** Values 1.0–1,500.0 NPR/kg, inside the 0–10,000 band. **Three spot-checks against fresh API calls, all exact**, spanning both resources and nine years: Cauli Local 2013-06-16 (30/35), Potato Red 2018-07-04 (36/38), Tomato Small(Local) 2022-04-18 (25/30). De-duplication dropped **3,190** rows and **every duplicated commodity-day agreed exactly** between the two resources — further evidence the positional mapping is right. Recovery needed no deletion: the loader skips values it already holds, so the re-run inserted only the missing 18,494 under a new release. **Storage came in at 28.5 MB against a 76 MB projection** (200.3 → 228.8 MB, ~186 bytes/observation): the projection divided the whole observations table and its indexes by row count, which over-attributes shared index overhead. Database at **228.8 MB of 500 MB (46%)**. `make lint` clean · `make test` **219 passed** (27 new) · `cd web && npm run build` green. | The **frontend panel is not built yet** — the data is in the warehouse and on the API but invisible on the site; that is the rest of ODN.S2 (commodity picker, daily line with a monthly-average toggle, CSV, provenance naming BOTH the market board and Open Data Nepal). Then: the series could be extended past April 2022 from the market board's own site (a different channel); the other 106 commodities and the per-piece/per-dozen rows sit in the raw lake, neither loaded nor lost. Longer tail unchanged: municipality map, place-first browsing, 164 economy indicators in "Everything else", and the unanswered World Bank email. |
| 2026-08-06 | **ODN.S1 — reusable CKAN client, raw-first; and the Kalimati dataset is not what the step file thinks** | Founder picked Kalimati daily prices as the next source (breadth over depth: a fourth publisher, daily data, instantly understandable). S1 builds the reusable half: `ingestion/opendatanepal/ckan_client.py` — `package_show`, `package_search`, paged `datastore_search`, and `store_raw` archiving every untouched page **plus** the package metadata (licence, agency, notes, modified) as one raw-lake snapshot before anything is parsed. Written against CKAN, not against ODN, because many government portals are CKAN — the P2B.S11 template paying off. Three guarantees: `success != true` is fatal (CKAN answers HTTP 200 on real errors); a harvest that ends short of the reported `total` **raises** rather than returning a short list, because a truncated harvest is indistinguishable from a small dataset once loaded; and field names pass through exactly as the source spells them. **Probing the source before building on it found two errors in the step file's own facts bank.** (1) The second Kalimati resource is not "May 2021 → present": its title says *to September 2023* and its first row is 2021-01-05, so Kalimati is a **closed historical series ending 2023**, not the live daily feed the step assumes — the ODN metadata's June 2026 modified date misleads. (2) That resource is **malformed at source**: uploaded with no header row, so CKAN made column names out of its first data row — the fields are literally `"Tomato Big(Nepali)", "2021-01-05 00:00:00", "Kg", "50", "60", "55"`. The positions do line up with commodity/date/unit/min/max/avg, but that mapping is **deliberately not adopted** — reading 45,250 rows through an unverified assumption is exactly what rule 1 forbids, so it is recorded as an open ODN.S2 decision to be settled against kalimatimarket.gov.np's own published prices. All findings written to a new `reference/opendatanepal/PROVENANCE.md`. | `make lint` clean · `make test` **192 passed** (15 new) · `cd web && npm run build` green. Live probe (`make odn-probe`) confirms the channel and the authority check: agency = Kalimati Fruits and Vegetable Market Development Board, licence **cc-by** (open, no sign-off needed), origin kalimatimarket.gov.np, two datastore-active resources totalling **242,411 rows**. The facts bank's sample row reproduces **exactly** — `Tomato Big(Nepali), 2013-06-16, Kg, 35.0, 40.0, 37.5` — and is now pinned by a test against a captured fixture, so a silent re-upload fails instead of passing as the same data. `store_raw` is tested against a LOCAL lake, never the real one (the raw lake is immutable; a test must not write into it), checking that every page plus the metadata is archived and that each member comes back byte-for-byte including Devanagari. Also noted for whoever debugs it next: the API's cert chain fails on a stale CA bundle (system Python) while curl and the project venv both succeed — nothing to fix here. | ODN.S2 (the load) with two decisions open and now visible: **whether to load the malformed resource at all** — resource 1 alone is a clean 2013→2021 daily series — and **storage**, since 242,411 source rows filtered to a Kg-only top-25 basket across three statistics is a large load for a free tier; the step mandates a projected-size check with a STOP at 200 MB. Then the frontend panel. Longer tail unchanged: municipality map, place-first browsing, the 164 economy indicators still in "Everything else", and the unanswered World Bank email. |
| 2026-08-06 | **WBF.S4 — update watcher: the fiscal source now tells us when it changes (WBF closed)** | The step proposed watching the dashboard page's "Last Updated" string. **That string does not exist** — S1 established it and `PROVENANCE.md` records it (the recon's own "Last updated: Feb 12, 2026" could never be re-confirmed from the page text). Building it anyway would have produced a watcher that never fires *while looking like it works*, which is worse than none. So the watcher checks **the data instead**: `fiscal_watch.py` re-harvests the nine federal aggregate series — calling the loader's own `harvest()`, so there is no second parser to drift — and reduces them to a fingerprint in `reference/wb-fiscal/watch_fingerprint.json`: per series, how many years, the newest year, and that year's value. A new fiscal year, a revised headline figure, or a renamed series all move it. `.github/workflows/wb-fiscal-watch.yml` runs quarterly (annual publication + quarterly check = at most one quarter stale, for ~36 requests a year), touches no database, needs no secrets, and **opens a labelled GitHub issue** rather than only turning red — a red run months later is easy to miss, an issue is not. Federal only, deliberately: a provincial harvest is ~45 minutes and rides the same publication cycle, so federal is the canary at nine requests. An unexpected period label stops the run rather than guessing which year is newest. | `make lint` clean · `make test` **177 passed** (14 new) · `cd web && npm run build` green. Verified against the live source three ways, not just unit-tested: **(1)** baseline written from a real harvest — 9 series, 7 years each, latest FY 2023/24, values matching the site (revenue 979,141.2, expenditure 1,121,154.0, debt 2,430,184.3 NPR million); **(2)** re-run reports `No change`, exit 0; **(3)** the alarm was **proved to fire** by perturbing the stored baseline (older year, altered value, deleted series) and re-running — exit 1, and it named all three in plain language: `NEW YEAR FISCAL_REVENUE_ACTUAL: FY 2022/23 -> FY 2023/24`, `REVISED FISCAL_DEBT_STOCK`, `NEW SERIES FISCAL_FINANCING_BUDGET`. Baseline restored afterwards and re-checked. **Known blind spot, recorded rather than hidden:** a revision to an OLDER year that leaves the newest year untouched does not move the fingerprint; keeping every value would make the file a second copy of the warehouse, and since both loaders are idempotent a re-run after any fire picks up older revisions anyway. | **WBF is complete (S1–S4).** Backlog candidates the founder weighed: Kalimati daily food prices (ODN.S1–S2, a new source and the most relatable dataset left), official boundaries + the 753-municipality map (NGP.S1–S2), and place-first browsing ("everything about Bagmati"), which the provincial data now makes worth building. Also open: "Everything else" still holds 164 of 647 economy indicators; and the World Bank has still not answered the founder's email on what the federal aggregate row represents (blocking the withheld revenue categories) or on the licence, still carried as assumed-unverified. |
| 2026-08-06 | **Province comparison on the Economy page + navigation: shelves instead of a wall, and a landing page that points somewhere** | Founder asked for the province comparison and for the data to be easier to navigate. Three things shipped. **(1) A real API bug, found before building on it.** `/v1/data/geo` — the one call a choropleth or a cross-province comparison makes — returned EVERY year at once under a single year's label: 35 rows for 7 provinces, stamped `FY 2019/20`. `is_latest` marks the newest *release* of a cell, not the newest year, so every year carries it. Census data hid this for months because a census has one year; the six-year provincial fiscal series is the first data at this level where it bites. Fixed by picking the newest period via `time_periods.sort_key` (not the label — `FY 2019/20` sorts after `FY 2023/24` in some spellings), in a pure `latest_period_rows` helper so it is testable offline like everything else. A geography missing that year is left absent rather than back-filled from an older one: blank on a map beats two years in one picture. **(2) `ProvincePanel` on the Economy page.** Form chosen for the job: seven named entities, one measure, one year is magnitude-across-categories, so a horizontal bar chart sorted by value — not the lines used upstairs, which answer change-over-time. One measure at a time means ONE series, so one hue, not seven: the hue follows the measure and matches the federal panel's entity colours, so colours learnt upstairs still hold. Measure and Budget/Actual are explicit segmented controls; table view, CSV and provenance below. **(3) Navigation.** Economy's 647 indicators were one wall grouped only by source — searchable only if you already knew the name. Now themed shelves with counts and a jump row, defined as reviewable data in `lib/sectors.ts` alongside the sector definitions; sectors without themes keep the old source grouping. The landing page gained a "Start here" band: four curated entry points with what is in each and who published it. | `make lint` clean · `make test` **163 passed** (5 new, on the period-picking helper) · `cd web && npm run build` green — `/[sector]` 6.5 kB, both panels still lazy-loaded so the seven non-Economy sectors pay nothing for ECharts. Verified in a browser against a local API, and **looking at the render caught three defects the build could not**: bar labels mixed `44.5` with `28`, so a column of numbers read ragged (now always one decimal); the theme matcher shelved every "(% of GDP)" series under national accounts — 106 of them, mostly trade and credit — because it matched the DENOMINATOR (now the bracketed unit is stripped before matching, and the broad shelf runs last); and "cpi" matched the World Bank's CPIA policy *ratings*, filing them under prices (now matched at word starts, and "cpi" dropped since "price" already catches both). Final shelves: Prices 33, Government finance 86, Trade 98, External 81, Money 36, Poverty 18, Business 62, Growth 69, Everything else 164. Province chart read back correct against the warehouse: Bagmati 44.5, Koshi 28.0, Sudurpaschim 18.5 bn. | "Everything else" still holds 164 of 647 — the shelves want another pass, and that number is deliberately visible so it cannot be ignored. Place-first browsing ("everything about Bagmati") is still absent and is the natural next navigation step. Then WBF.S4 (update watcher). |
| 2026-08-06 | **WBF.S2 (provincial) — closed properly: a false "loaded" claim corrected, the data actually loaded, and the independent check done** | Session opened on a status question and found a discrepancy worth more than the work it interrupted: `reference/wb-fiscal/PROVENANCE.md` stated the provincial data was **loaded on 2026-08-04**, and it was not. The warehouse held **zero** provincial observations and not one provincial indicator — the loader had been written and tested but no successful load had ever run. Committing that file as written would have published a claim about our own provenance that was untrue, which is the same failure as misreporting a source. The claim is **corrected in place, not deleted**, with the reason recorded. Then the actual work: ran the loader for real, and did the independent spot-check WBF.S2 asks for and the earlier session had left open. **Two provinces checked against their own announced budgets for FY 2023/24 (BS 2080/81), not one.** Bagmati: capital 35,506.3 vs our 35,506.3, current 26,702.7 vs our 26,702.8. Koshi: capital 18.23 bn vs our 18.23, current 14.39 + local transfers 3.60 = 17.99 vs our recurrent 18.00. In **both** provinces our total is short by exactly the province's "financial management / fiscal arrangement" line (500.0 and 10.0) — that is financing, which the dashboard keeps in a separate sheet we do not load; add it back and the totals agree. So the residual is explained rather than tolerated. The check also settled a mapping that had been an assumption: the dashboard's **"Recurrent expenditure" includes a province's fiscal transfers to local levels**, not just its own running costs (Koshi 17,999.3 = 14,396.3 + 3,603.0). Sources are press reports of the two budget speeches, cited in PROVENANCE with the limitation stated plainly: the ministries' own PDFs were tried first and are unusable here (Koshi's annual report is a scanned image; the economic survey's Devanagari font breaks on text extraction). Also logged WBF.S3b, whose session ended without a diary entry. | **1,078 observations loaded under release 47**, read back from the warehouse: 6 indicators x 7 provinces, Actual FY 2018/19–FY 2023/24 and Budget FY 2019/20–FY 2023/24 (the shorter budget window is the source's own gap, not a harvesting loss — the cross-check would have caught a dropped year). **Duplicate latest cells: 0.** Re-run for FY 2023/24 loaded **0** and skipped **196** — idempotent. The two cross-checked figures read back exactly: Koshi 36,233.5, Bagmati 62,209.1. The source's own all-provinces cross-check passed for **every category, year and type** (e.g. FY2024 taxes sum to 84,969.36). Live API already serving it: `/v1/data?indicator=FISCAL_PROV_EXPENDITURE_BUDGET&geo=NP01` returns the series. `make lint` clean · `make test` **158 passed** · `cd web && npm run build` green (`/[sector]` 5.8 kB — panel still lazy-loaded). Operational note for WBF.S4: the World Bank's Tableau host is **slow and can hang** — a full 7-year harvest is ~45 min, not the 6 min the docstring claims, and one run stalled for over two hours before being killed. The retry helper added to `fiscal_acquire.py` survives transport blips but cannot rescue a server that accepts and never answers; the watcher needs a wall-clock cap. | Provincial data is loaded but **not yet visible anywhere on the site** — the FiscalPanel is federal-only, so the province comparison (the strongest pitch asset: per-province budgets are hard to find anywhere) still needs a frontend pass. Then WBF.S4 (update watcher). Minor, pre-existing, unrelated to this session: `/v1/data` reports `indicator.source = null` for the fiscal indicators, federal ones included — worth a look when convenient. Still open with the World Bank (founder's email): what the federal aggregate row represents, which would unlock the withheld revenue categories, and the licence, still carried as assumed-unverified. |
| 2026-08-01 | **WBF.S3b — the panel filled out: six charts, all nine federal series** (logged late, on 2026-08-06 — this session ended without its diary entry) | S3 shipped three charts covering four of the nine loaded series: financing and net operating balance were in the warehouse but appeared nowhere on the Economy page, and expenditure budget-vs-actual existed only in the table. Now six charts, paired: revenue vs expenditure, net operating balance, revenue plan-vs-outturn, spending plan-vs-outturn, financing, debt stock; the table carries all nine series across 10 columns. Financing and the operating balance go negative, so their axes are anchored at zero (`scale:false`) — an axis floating free of zero makes a wholly negative series read as an ordinary rise and fall, and for a financing *requirement* the sign is the whole point. The palette holds one hue per entity across every chart and never cycles (revenue series-1, expenditure series-2, financing series-3, debt series-4, operating balance the brand crimson); budget-vs-actual reuses the entity hue and separates by dash, so no chart carries more than two series. | `cd web && npm run build` green — `/[sector]` still 5.79 kB, so the panel stayed lazy-loaded and the seven non-Economy sectors still pay nothing for ECharts. Commit `8502589`. | Provincial fiscal data — believed at the time to be loaded; it was not (see the 2026-08-06 entry). |
| 2026-08-01 | **WBF.S3 — the public finance panel: fiscal data made visible, not just present** | The data was live but buried in a 676-item list on `/economy`, which is where fiscal data goes to never be found. Added `FiscalPanel` above that list: four stat tiles (revenue, expenditure, the gap, debt stock), three charts, a table view, CSV download, and a provenance block naming WB plus MoF / Provincial MoFs / FCGO / NRB with the FY window and the verification result. Followed the dataviz method rather than taste: **form first** — every series is change-over-time so every chart is a line, and **no dual axis** anywhere (debt stock has a different magnitude, so it gets its own chart instead of a second y-scale); **colour by entity in the portal's already-validated categorical order** (revenue = series-1, expenditure = series-2, debt = series-4), never re-ordered; budget-vs-actual is the SAME entity measured twice, so it reuses the entity hue and separates by dash pattern rather than spending a new hue. Figures are stored in NPR million and displayed in NPR billion (exact ÷1000), labelled as such on every axis, tooltip and tile. | `make lint` clean · `make test` **149 passed** · `cd web && npm run build` green. Verified in a browser against a local API: all four tiles populate (revenue NPR 979.1 bn, expenditure 1,121.2 bn, gap 142.0 bn, debt 2,430.2 bn for FY 2023/24), three canvases draw at the right sizes, and the budget caption computes the story — **budgeted NPR 1,298.6 bn, collected NPR 979.1 bn, NPR 319.4 bn less than planned**. Two real defects found by looking at the render and fixed: the final x-axis label `FY 2023/24` was clipping (grid right padding 24 → 48), and the budget chart's legend drew both series as identical solid swatches, putting identity back on colour alone — legend icon changed from `roundRect` to `line` so the swatch reproduces each series' dash pattern. **A third defect caught by the build:** a static import of `FiscalPanel` pulled ECharts onto every sector page (`/[sector]` 5.74 kB → **183 kB**); switched to `next/dynamic` with `ssr:false`, back to **5.79 kB**, so the seven non-Economy sectors pay nothing. | Provincial fiscal data is harvested and modelled but still not loaded — it is the strongest remaining pitch asset (per-province budgets are hard to find anywhere) and would add a province comparison to this panel. Then WBF.S4 (update watcher). Still open with the World Bank (founder's email): what the aggregate row represents, which would unlock the withheld revenue category breakdowns, and the licence, still carried as assumed-unverified. |
| 2026-08-01 | **WBF.S2 (part 2) — blocker resolved against Nepal's own accounts; federal fiscal series LOADED (public finance is live)** | Broke the aggregate-vs-parts tie with a primary source rather than a preference: the FCGO Consolidated Financial Statements (the compiler's own cited origin). Two independent years confirm the dashboard's **aggregate** row is federal **non-financing receipt** — FY 2018/19 765,535.7 vs FCGO 764,767.76 (**0.10%**), FY 2022/23 913,786.1 vs 910,370.97 (**0.38%**) — and expenditure matches FCGO recurrent+capital for FY 2018/19, 957,980.1 vs 944,351.58 (**1.44%**). A revenue-sharing explanation for the gap fitted FY 2018/19 to 0.12% (WB gap 97,029.0 vs FCGO sharing 97,146.57) but **failed** for FY 2022/23 by 12%, so it was NOT adopted. Consequence: the verified aggregates are loaded and the revenue **category** rows are deliberately withheld — publishing four numbers that add up to a fifth contradicting the published total would be the exact trust failure rule 1 exists to prevent. Seeded `NPR_MILLION` (read from the source's own `Local Unit` column), the `Nepal Fiscal Dashboard` dataset (licence carried as assumed-unverified), and 9 curated indicators; Budget and Actual are separate series, never mixed. `fiscal_pipeline.py` loads only what is verified and carries a sanity band that would stop a 1000× unit error. | Loaded **63 observations** under release 46 across 7 fiscal years (FY 2017/18 – FY 2023/24, BS 2074/75 – 2080/81). Re-run loaded **0**, skipped 63 — idempotent. Duplicate check clean: one latest row per series-year. Warehouse read back and re-checked against FCGO at **0.10% / 0.38% / 1.44%**. Series behave: debt stock rises 917,052.9 → 2,430,184.3; financing is negative throughout (a financing requirement); budgeted expenditure exceeds actual in every year. `make lint` clean · `make test` **149 passed** · `cd web && npm run build` green. **Nepal's public finances are now on the portal** — the empty sector from the 2026-07-19 recon is filled. | Provincial data is harvested and modelled but **not loaded**: each per-province figure is the source's own published number and the 7 provinces sum exactly to the national total, so it is loadable, but it needs its own review pass. Then WBF.S3 (a public-finance panel on the Economy page) and WBF.S4 (update watcher). Still open with the World Bank (founder's email, question 3): what the aggregate row represents, which would unlock the withheld category breakdowns; and the licence, still carried as `CC BY 4.0 (assumed, WB terms — verify)`. |
| 2026-08-01 | **WBF.S2 (part 1) — federal + provincial modelled and harvested; the load is BLOCKED by a real discrepancy in the source** | Founder chose federal + provincial (municipality-level deferred: 753 units ≈ 26,000 requests ≈ 7 h of polite harvesting). Read the mappings from both ends rather than assuming them: the dashboard's seven province spellings match the warehouse's `NP01`–`NP07` names exactly, and the fiscal-year periods (`FY 2017/18` = BS 2074/75) are already seeded. Resolved the fiscal-year labelling ambiguity the step file flagged — the dashboard labels by the **ending** Gregorian year, so its `FY2018` is Nepal's FY 2017/18; derived from two independent magnitude checks (federal revenue level, and provincial agriculture GVA on the GDP sheet) and recorded in `fiscal_layout` as derived-and-still-to-be-confirmed. Wrote `fiscal_layout.py` (registry + province map + year mapping + `parse_amount` that refuses to turn `*` into `0`) and `fiscal_harvest.py`, which **writes no database rows at all** — its output is a staging file behind the founder review gate. | `make lint` clean · `make test` **149 passed** (was 138; 22 new across layout and acquisition) · `cd web && npm run build` green. Registry facts measured, not assumed: Federal Debt Stock returns **zero rows** for `Type1=Budget`, so it carries actuals only; provincial sheets enumerate their own Group2 categories. Completeness is asserted in tests via two exact sums recorded from the source (7 provinces → 84,969.36; 4 federal revenue categories FY2018 → 766,036.1, both diff **0.0000**). | **BLOCKED, deliberately — nothing loaded.** The sum gate caught a genuine inconsistency: on Federal Revenue the aggregate row (`Group2 = *`) equals its four categories **exactly in FY2018 and in no other year**, with the parts exceeding it by 97,029 → 126,867 (12–13%) in FY2019–FY2024. The aggregate is also near-flat across FY2018–FY2020 (766.0 / 765.5 / 764.7 bn), implausible for Nepal's revenue, while the parts sum moves as revenue actually did. Ruled out: parsing (FY2018 reconciles through the same code), the FY mapping (would shift years, not levels), and simple nesting (no subset reproduces the aggregate). Publishing the aggregate would understate federal revenue by ~12% in six of seven years; publishing the summed parts would state a total the source does not. Full write-up in `reference/wb-fiscal/PROVENANCE.md`. **Resolve by (a) asking the World Bank — added as question 3 to the founder's drafted email — and/or (b) spot-checking one year against the MoF Red Book.** Then load, with the resolution recorded in each indicator's `definition_en`. |
| 2026-08-01 | **WBF.S1 — fiscal dashboard channel spike: it works, and far more easily than feared** | The riskiest acquisition in the backlog turned out to be the simplest. The recon expected the full Tableau session protocol (`X-Session-Id` → `bootstrapSession` → length-prefixed JSON) and warned `<view>.csv` "returns an HTML shell, not data". That is true only for a **cold** request: the shell is what you get without a cookie. Two plain calls on one `requests.Session` — GET the view (server sets cookies), then GET `<view>.csv` — return real `text/csv`. No browser, no session id, no JSON parsing. The protocol was found by loading the workbook in a real browser and reading the network log and its live `tsConfigContainer`, rather than guessing endpoint shapes. Wrote `ingestion/worldbank/fiscal_acquire.py` (fails loudly on a non-CSV response rather than parsing a shell, one polite call per second, retries once on a lapsed session), `make wb-fiscal-acquire`, and `reference/wb-fiscal/PROVENANCE.md`. | **All 16 data sheets harvested, 0 failures**, archived to the raw lake as one snapshot (`worldbank/fiscal-dashboard/2026-08-01T094608_360732Z/sheets.json`, sha256 `1717e4f7…`, 14,240 bytes) with each member's own bytes and hash. `make lint` clean · `make test` **138 passed** (was 131; 7 new) · `cd web && npm run build` green. **Inventory is bigger than the recon recorded: 18 sheets across THREE tiers — federal, provincial AND local** (recon said federal + provincial), including federal Debt Stock, provincial + local Grants, and Provincial GDP. **The unit was read, not assumed:** the sheets carry a `Local Unit` column whose literal value is `NPR Million` — the classic 1000× fiscal error avoided by reading the source. `Type1` separates `Actual` from `Budget` (FY2018 federal revenue: budget 802,223.22 vs actual 766,036.1), so they become separate indicators. URL filters verified working — `Fiscal year`, `Province Name`, `Group2` (hierarchy drill) and `Type1` all compose — so the full grid is reachable in S2. | **Two things S2 must handle, both recorded in PROVENANCE.md.** (1) **View state IS the data**: a sheet's CSV is only what its current view renders — federal defaults to all 7 years at the top of the hierarchy, but provincial/local default to FY2024 aggregated over all places. S2 must walk the filter grid, not just harvest once. (2) **A wrong filter value is silent**: `Province Name=Bagmati` returns data, `Bagmati Province` returns **zero rows with no error** — indistinguishable from "no data", so an unmatched value must fail loudly in S2. **Licence NOT verified and recorded as such**: the WB page carries no licence statement at all (no "Creative Commons", no "CC BY"), so it is carried as `CC BY 4.0 (assumed, WB terms — verify)`; the page invites contact at Infonepal@WorldBank.org, which is the way to settle it. First figures are preliminary and **not yet spot-checked** — S2 must verify federal revenue and expenditure for one FY against the MoF Red Book before anything is published. |
| 2026-08-01 | **SRCH.S1 — global search across the whole warehouse (new step, founder-raised)** | The founder pointed out there was no way to search the portal; confirmed before building — a search of `api/` for any query parameter, `ILIKE` or full-text function returned zero matches, so there was no search at all, not a weak one. Wrote `docs/steps/global-search.md` and slotted it into `START-HERE.md` as item 6, ahead of the remaining source onboardings, on the reasoning that search multiplies breadth already paid for and every later source then becomes findable the day it lands. Built `GET /v1/search` over indicators and geographies: one `UNION ALL`, a `CASE` relevance ladder (exact code 100 → code contains 80 → name starts-with 60 → name contains 40 → definition 20), and the same `EXISTS (observations)` filter `list_indicators` uses, so search never advertises an indicator the portal cannot chart. **Deliberately no migration, no index, no extension** — the two dimension tables hold ~2,700 short rows where a sequential scan is sub-millisecond, and storage is the scarce resource. **Deliberately `ILIKE`, not `to_tsvector`** — full-text stems against a language config, no Nepali config ships with Postgres, and an `'english'` vector would make `name_ne` dead text; character matching is script-agnostic. Added `escape_like()` because an unescaped `%` typed by a user would otherwise match the entire catalogue. Front end: a header search box (`/` focuses it, hidden under 860 px), a `/search` results page grouping hits into Datasets and Places, and `hrefForHit` routing indicators to `/explore?indicator=` — or `/banking` for the NRB monthlies that `/explore` filters out — after reading the components to confirm both already accept a code from the URL. | `make lint` clean · `make test` **131 passed** (was 122; 9 new search tests) · `cd web && npm run build` green, `/search` 2.77 kB. Verified against the live Supabase warehouse: `literacy` → **15** matches, `cooking fuel` → **11**, `Sarlahi` → the district, `बागमती` → Bagmati, `इलाम` → Ilam, exact code `CENSUS_LITERACY_RATE` → that row first. Escaping holds: `%%` → **0** results, not the whole catalogue. Guards behave: a 1-character query → 422, `zzzznothing` → 200 with `total: 0` (no match is an answer, not a 404). End-to-end in a browser: typed "cooking fuel" in the header on `/explore` → results page → clicked "Cooking fuel: firewood" → the chart showing **3,398,316** households, which matches the figure recorded in this log on 2026-07-27 exactly. Fixed one real UI bug found only in the browser: the shared `.state` class is a centred flex column, which had fragmented the no-results sentence across six lines. **No migration and no measurable DB growth.** | **Two gaps found and reported, not papered over** (both recorded in the step file). (1) Nepali text exists for the country, 7 provinces and 77 districts, but for **0 of 753 municipalities and 0 of 2,252 indicators** — so Nepali search reaches provinces and districts only; filling `name_ne` for municipalities is human-curated seed work (rule 4). (2) Kathmandu is stored `काठमाडौँ` with **chandrabindu**; a user typing `काठमाडौं` with **anusvara** gets nothing. Both spellings are in common use, but declaring ँ and ं interchangeable is a judgment about Nepali, not code — **needs the founder's decision** before any normalization is written. Then the founder's chosen next source: **WBF.S1–S3 fiscal series** (Tableau spike first, has a fail-safe STOP). |
| 2026-07-27 | **P2B.S7 — Indv22 place of birth (migration) loaded; eighth census table live** | Loaded `Indv22_PopulationByPlaceOfBirth` in balanced mode using the sum rules added earlier the same day, curated its 10 display names, and gave it a "Migration and place of birth" group in the `/population` picker. The `nb_othrDist_M/H/T/NS` suffixes are undefined in Indv22 itself; rather than guess or leave raw codes, they were read from the same census's `Indv26_MigratedPopulationByEcoBelt`, whose columns spell the vocabulary out in the same order (Mountain, Hill, Tarai, NotStated) with `NS` an exact abbreviation match. Each affected indicator's `definition_en` records that derivation so the reasoning stays auditable. | Loaded **50,880** cells; re-run loaded **0**, skipped 50,880. Warehouse matches the source NEPAL row exactly: total **29,164,578**, born in Nepal **28,420,333**, same municipality **20,117,395**, another municipality **2,612,713**, another district Hill **3,644,722** / Tarai **1,399,099** / Mountain **639,769** / belt not stated **6,635**, born abroad **734,663**, not stated **9,582**. Belt magnitudes corroborate the label derivation (Hill > Tarai > Mountain matches Nepal's distribution). Observations **295,870 → 346,750**; DB **191 MB** of 500 MB. `make lint` clean · `make test` **122 passed** · `cd web && npm run build` green. | **Eight census tables are now live.** Remaining honesty gap: four labels still show raw source codes the census does not define — Hhld09 `a_FlushToilet1`/`b_FlushToilet2` and Hhld10 `d_MobilePhone1`/`e_MobilePhone2`; confirm against the printed NPHC table. Municipality-level cross-tabs remain out of scope (~550 MB). Next source, not next census table, is the stronger funding story — fiscal (WBF), Kalimati prices (ODN), or elections (ECN). |
| 2026-07-27 | **P2B.S7 — sum rules moved into the manifest; Hhld10 household facilities loaded** | The loader hardcoded one shape (every category column sums to `rowtotal`), true of 84 tables and wrong for two, so those two could not load. Added `SumRule(total, parts)` to the compiler, manifest and loader: each file now declares its own arithmetic, flat tables get the default identity stated explicitly, and a manifest without `sum_rules` is rejected rather than silently falling back. Curated rules for the two hierarchical tables, each verified against the file's own NEPAL row first: `Hhld10` — `rowtotal = x_NoFacility + atleastOne`, with `a_Radio…n_AirConditionr` multiple-response and deliberately not sum-checked; `Indv22` — `rowtotal = allNativeBorn + fBorn + notstd` and `allNativeBorn` = the six `nb_*` columns. Also added `_keep_curated_names()`: regenerating the manifest rewrites the indicator seed, which would have silently reverted yesterday's 49 curated display names to raw column codes. Loaded Hhld10, curated its 17 labels, and gave it a "Household facilities" group in the picker. | **All 86 files now validate** (was 84). Hhld10 loaded **14,246** cells; re-run loaded **0**, skipped 14,246. Warehouse matches source exactly: Nepal households **6,660,841**, no facility **258,540**, at least one **6,402,301** (identity holds), computer **998,478**, internet **2,514,510**. Observations **281,624 → 295,870**; DB **182 MB** of 500 MB. Manifest regeneration kept all **49** curated names. `make lint` clean · `make test` **122 passed** · `cd web && npm run build` green. Reported, not guessed: Hhld10's `d_MobilePhone1` / `e_MobilePhone2` are two published categories whose distinction the source does not state, so both keep their raw code visible, as with Hhld09's two flush-toilet codes. | `Indv22_PopulationByPlaceOfBirth` validates (**50,880** cells, ~16 MB) but is **not loaded** — migration is a strong Nepal story, so it is the obvious next table if the storage is wanted there. Then deploy: **eight commits** are unpushed on `census-load`. |
| 2026-07-27 | **P2B.S7 — the six census tables made readable on `/population`** | The data was already reachable without any code change: `/v1/indicators` lists only indicators that have observations, and `get_geo_values` already filters to the headline (`breakdowns = '{}'`) row, so all 49 new indicators had 838 values at four levels the moment they loaded. What was missing was legibility — the picker showed **56 flat entries named after source column codes** (`Population By Edu Level: c_lowrsecndry`). Curated display names for the 49 indicators in `db/seeds/indicators_census_bulk.csv`, read literally off the source column codes, and grouped the picker by source table (Literacy · Education level · Disability · Age and sex · Cooking fuel · Toilets) with headline indicators first and each table's own total above its categories. Unmatched codes fall back to the headline group so a newly loaded table can never silently vanish from the picker. | Verified in a browser against a local API: `/population` renders **Literacy: cannot read or write** across **77 districts** — Sarlahi **306,128** highest, Manang **1,021** lowest — with CSV download and district drill-down working. Re-seeding updated names idempotently (861 indicators). `make lint` clean · `make test` **119 passed** · `cd web && npm run build` green. Reported, not guessed: Hhld09's `a_FlushToilet1` / `b_FlushToilet2` are undefined in the source and could not be resolved from published NPHC documentation (plausibly sewer vs septic tank, unverified), so both keep their raw source code visible in the label. | Confirm the two toilet labels against the printed NPHC table, then fix the two hierarchical files (`Hhld10_HouseholdFacility` carries the internet/mobile/computer digital-divide story). Push to `master` when ready to deploy — three commits are waiting on `census-load`. |
| 2026-07-27 | **P2B.S7 — bulk census loader committed, idempotency bug fixed, six census tables loaded** | Committed the manifest-driven bulk loader written in a Codex CLI session on 2026-07-26 (`scripts/census_bulk_manifest.py`, an 86-file manifest, 854 curated indicators, `ingestion/nso/census_bulk.py` with full/balanced/headline modes). Verification found a real idempotency bug: the change check compared Postgres' rendered `breakdowns::text` against Python's `json.dumps`, and jsonb orders keys shortest-first-then-bytewise, so no multi-key breakdown ever matched and every re-run reloaded the whole file. Both sides now normalise through `breakdown_key()` and the stored mapping is read as jsonb, not text; regression test added. Storage drove scope: `full` is ~21.25M cells (~7 GB) and `balanced` ~2.57M (~860 MB) against a 500 MB free tier, so **six tables** were loaded in balanced mode (~78 MB) rather than all 86 — deliberately keeping room for other sources. Deleted the pre-fix rows and reloaded clean. | Dry run: **84 of 86 files validate**, 2,565,041 cells balanced / 689,261 headline. Pre-fix re-run reloaded **170,680** unchanged cells (bug); post-fix re-run loads **0** and skips **216,842** unchanged across all six. Loaded: cooking fuel 6,704 · toilet 5,028 · five-year age 7,614 · disability 76,320 · literacy 35,640 · education level 85,536 = **216,842**, 0 failures. Spot-checks match source CSVs exactly: Nepal cooking-fuel total **6,660,841**, firewood **3,398,316**, LPG **2,949,995**; Nepal read-and-write **20,377,980**. Observations **64,782 → 281,624** (no duplicates). DB **179 MB** of the 500 MB tier (includes delete bloat; a `VACUUM FULL` would reclaim ~47 MB). `make lint` clean · `make test` **119 passed** · `cd web && npm run build` green. Two files reported, not guessed — both hierarchical, needing a per-file column-hierarchy declaration: `Hhld10_HouseholdFacility` (`atleastOne` is a subtotal; facility columns are multiple-response) and `Indv22_PopulationByPlaceOfBirth` (`allNativeBorn` subtotals the `nb_*` columns). | Surface the six new topics in the web UI — the data is in the warehouse but `/population` does not yet expose literacy, education, disability or the household topics; that is what turns this into a demonstrable portal. Then fix the two hierarchical files (Hhld10 carries the internet/mobile/computer digital-divide story). |
| 2026-07-26 | **P2B.S7 — Hhld05 household floor-material census loaded at all four geography levels** | Added the curated `CENSUS_HH_FLOOR_MATERIAL` indicator and `make ingest-census-floor-material`. Extracted the verified Hhld06 mechanics into `census_shape_a.py`, a reusable strict Shape-A engine for the remaining simple household tables: raw-first archive, exact header/category-sum checks, curated numeric geography mapping, expected-level reconciliation, quality gate, batched change-only writes, and release/ingestion lineage. Hhld05 remains an explicit small configuration and preserves its six source column codes verbatim in `breakdowns.category`; Hhld06 was regression-tested through the same engine. | Source reconciliation: **1 country + 7 provinces + 77 districts + 753 local units = 838 geographies**; every level sums to **6,660,841 households**. First run loaded **5,866** observations (headline + 6 categories); second run loaded **0** and skipped **5,866** unchanged. Warehouse spot-checks match the file: Nepal **6,660,841**, Taplejung **27,776**; latest uniqueness Hhld05 **5,866/5,866**, Hhld06 **8,380/8,380**; releases 16 and 17 carry immutable raw-file refs. `make lint` clean · `make test` **115 passed** · `cd web && npm run build` green. | Continue Tier 1 with **`Hhld07_TypeOfCookingFuel.csv`**, one file per task, following `CODEX_TASKS.md`. |
| 2026-07-26 | **P2B.S7 — Hhld06 household drinking-water census loaded at all four geography levels** | Added the curated `HOUSEHOLDS` unit and `CENSUS_HH_DRINKING_WATER` indicator, then built `ingestion/nso/census_drinking_water.py` plus `make ingest-census-drinking-water`. The Shape-A loader archives the untouched CSV before parsing, maps only through the numeric province/district references and official 753-unit crosswalk, rejects unknown/duplicate geographies and malformed totals, preserves all nine ambiguous source column codes verbatim in `breakdowns.category`, quality-gates before writes, batches inserts, and writes only changed cells. Added focused offline parser tests. | Source reconciliation: **1 country + 7 provinces + 77 districts + 753 local units = 838 geographies**; every level sums to **6,660,841 households**. First run loaded **8,380** observations (headline + 9 categories); second run loaded **0** and skipped **8,380** unchanged. Warehouse spot-checks match the file: Nepal **6,660,841**, Taplejung **27,776**; latest-row uniqueness **8,380/8,380**; releases 14 and 15 both carry immutable raw-file refs. `make lint` clean · `make test` **113 passed** · `cd web && npm run build` green. | Continue Tier 1 with **`Hhld05_FloorOfHouse.csv`**, one file per task, following `CODEX_TASKS.md`. |
| 2026-07-26 | **P2B.S8b — census drill-down UI (municipality map + district drill), deployed** | The municipality data from S8a is now visible. **API:** `/v1/data/geo` accepts `level=local_unit` and an optional `parent` (district P-code) → returns one district's local units in a single call (the drill-down query; the choropleth shape reused). **`/population`:** a third level **"By municipality"** paints all **753** units nationally; and a **district → municipality drill** — click a district on the map *or* a district name in the table (keyboard-accessible), get a focused map of just that district's local units with a breadcrumb (`← All districts / Municipalities of {district}`), its own ranking and CSV. **Level availability is data-driven, not hardcoded** (Master Prompt / step): an indicator with no municipality rows shows an honest "not available at this level yet" message instead of an empty map (only `CENSUS_POP_TOTAL` has municipality rows today). **`ChoroplethMap`** registers the COD-AB local-unit map (keyed on `ADM3_PCODE`), builds a district-filtered focus view on demand, and emits region clicks for drilling. | Live-verified: site `/population` HTTP 200; national municipality endpoint returns 753; **Kathmandu drill → its 11 municipalities** (Kathmandu 862,400, Dakshinkali 26,372). Screenshots founder-approved (national 753 map, Kathmandu drill, district view). `make lint` clean · `make test` **109 passed** (+3 geo/drill tests) · `cd web && npm run build` green (`/population` 92.8 kB, under budget). Deployed (API + site). | **P2B.S9+ / the long tail** (START-HERE #15), OR mine the rest of `Census_data/` — the other 88 topic CSVs give household facilities, disability, literacy, migration, occupation etc. at municipality level (unlocks P2B.S7 census families and richer municipality indicators; households `nHhld` is already in `Indv01`). Also outstanding: local-unit `name_ne` is NULL for all 753 (needs a Nepali source); the orbit's governance node still says "in preparation". |
| 2026-07-26 | **P2B.S8a — municipality (753 local unit) census: seeded, mapped, population loaded** (S8b UI done in the entry above) | **Found the right data source and the right endpoint.** Municipality data is served by the census API's **`/local-level-container/*`** family, NOT `/population/highlight` (which 500s at municipality — saved to memory). Then the founder supplied the full NSO bulk download (`Census_data/`, 89 CSVs — the whole census at every level). **`Indv01_PopulationBySex.csv` is the ideal source:** final, official, reconciles *exactly* (753 named local units 28,925,480 + 77 institutional rows 239,098 = 29,164,578 national; per district: municipalities + institutional = the district total — the earlier ~0.9% "gap" was institutional population). **Resolved the boundary name-crosswalk authoritatively** (the founder asked to verify, not guess): the census NSO names differ from the geoportal spellings for ~16 units, so verified against **OCHA COD-AB `npl_admin3`** — census→COD matched **753/753** (751 exact) and COD carries official **`adm3_pcode`** (`NP0101301`, extends our district P-codes). **Adopted COD-AB as the local-unit boundary + code source** (better than my geoportal-minted `LUCODE`): rebuilt `web/public/maps/nepal-local-units.json` from COD-AB geometry (753 features, ~557 kB, keyed on `ADM3_PCODE`, 10/10 containment); the geoportal harvest stands as independent verification. Seeded the **753 local units** into `geographies.csv` (level=local_unit, official `adm3_pcode`, parent district, `valid_from` 2017-03-10; `name_ne` NULL for all 753 — no Nepali source yet). Wrote `ingestion/nso/census_local_units.py` (`make ingest-census-local`): raw-first archive of the source CSV, quality-gated, idempotent load of `CENSUS_POP_TOTAL` (total + male/female) at municipality level. `local_unit_crosswalk.csv` and `nso_munis_raw.json` committed as reviewable reference; 220 MB `Census_data/` gitignored. | Load: **2,259 municipality observations** (753 × total/male/female), sum **28,925,480** (exact); spot-checks match official (Pokhara 513,504, Bharatpur 369,268). Seed: geographies **85 → 838**, all 753 with a valid district parent. Map: 753 features, 10/10 sample point-in-district. `make lint` clean · `make test` **107 passed** · `cd web && npm run build` green. Nothing pushed with a *visible* change yet — the data + map asset are in place but not rendered. | **P2B.S8b — the drill-down UI** (the visible piece): extend `/v1/data/geo` to serve local units **filtered to one district** (drill-down, not all 753 at once) and add `level='local_unit'`; `/population` gains a municipality level with district→municipality drill and national→province→district→municipality breadcrumbs; register `nepal-local-units.json` in `ChoroplethMap` (nameProperty `ADM3_PCODE`); per-topic level availability from the data. Then optionally load more `Census_data/` topics (households `nHhld` is already in `Indv01`; the other 88 files unlock P2B.S7). |  Built `scripts/geoportal_harvest.py` (`make geoportal-harvest`): pulls three admin layers from the Survey Department's open WFS (`base_nepal_local_level_new`, `_district_new`, `_province_new`) as GeoJSON, archives each **untouched in the raw lake** (raw-first) before parsing, then reconciles against our geography codes. **Resolved the 777-vs-753 puzzle by inspection, not assumption:** the local-level layer serves 777 features = 755 of the four municipality LTYPEs + 22 protected areas (parks/reserves — not local governments, excluded); the 755 include 2 duplicated split-geometry features (Bansagadhi/Bardiya, Binayee Tribeni/Nawalparasi_E) → merged → **exactly 753** official units. Districts reconcile **77/77** (the 4 split districts map via an explicit `_E→East`/`_W→West` alias, cross-checked against each feature's province — not guessed). Since the geoportal serves **no municipality code** (its `PCODE` is the province number) and NSO municipality ids are not yet extracted, minted a documented internal `LUCODE` (district P-code + 2-digit index) as the join key; wrote `reference/geo/geoportal/local_units_mapping.csv` (753 rows) + a full `PROVENANCE.md`. **NGP.S2:** the harvester emits a full-resolution 753-feature GeoJSON (duplicates' geometries merged), simplified with mapshaper (`-simplify 1.5% keep-shapes`, precision 0.0001) to **`web/public/maps/nepal-local-units.json`** — 753 features, ~592 kB, structure matching the existing district/province map files so `registerMap` accepts it unchanged. Carried the **pre-2020 boundary caveat** (the Darchula/Kalapani edge is pre-2020, inherited from this source) into both the file's `_provenance` and PROVENANCE.md. | 3 layers raw-archived (raw-lake `surveydept/geoportal/…`); `make geoportal-harvest` reconciles **77/77 districts, 753/753 local units** (fails loudly otherwise); web map **753 features, 592 kB (≤600 kB), 10/10** sampled units' representative point inside their district polygon. `make lint` clean · `make test` **107 passed** (+4 boundary-reconciliation tests) · `cd web && npm run build` green. No visible site change yet — this is the boundary foundation. | **P2B.S8 — municipality-level census + drill-down map UI** (the founder-visible municipality feature): join municipality census to `local_units_mapping.csv` by (district, name) — or first gather municipality NSO ids from the census API — and render `nepal-local-units.json` in ChoroplethMap with district→municipality drill-down. |
| 2026-07-25 | **P2B.S5 finish — decision 0005 in the UI + sector-page card redesign** | The sector portal shipped earlier, but finishing S4 left the live pages out of sync with the headline policy, and the founder flagged the sector lists as unattractive ("a few figures then a long list"). **Two parts.** **(1) Decision 0005 in the UI:** `/v1/indicators` now feeds the real `source` + `preferred_source`, so badges are authoritative not guessed; People & Education now lead "At a glance" with the **census** headline (Population 29.2M, Literacy 76.2% — Census 2021) instead of the World Bank modeled figures, and the demoted WB series carry an amber **"alternative estimate · World Bank"** badge in the list; `HeadlineChart` reads its source from the series provenance and renders single-year census facts as a clean value (no misleading one-point trend); removed the stale "arrives with P2B.S3" governance copy. **(2) Card redesign (founder-approved after research** — dashboard-UX best practice is a scannable card/small-multiples grid, not a flat list): replaced the list with a responsive **grid of indicator cards**, each showing the latest value, a **sparkline**, and the source/alternative badge, grouped by source with a count. To draw a value+sparkline on every card without one fetch per indicator, added a bulk API endpoint **`GET /v1/indicators/spark`** (one window-function query picks the headline breakdown per indicator+period and returns latest value + recent points); sparklines are **hand-drawn inline SVG** (no chart lib) so the route's first-load JS is unchanged. Big sectors (Economy ~600) are **capped at 24 cards per group with a "Show all N" toggle** so the page stays tight. | Verified on the live DB via a local API: `get_spark_series` returns 1,356+ spark rows in **505 ms** (one query); NRB monthly + single-year census both handled. `make lint` clean · `make test` **103 passed** (added spark-endpoint + badge tests) · `cd web && npm run build` green, all routes static, `/[sector]` **102 kB** (inline SVG added ~0). Screenshots reviewed and **founder-approved** (People/Education/Economy at 360/768/1440; Economy page height ~45,000px → ~4,300px with the cap). | **NGP.S1–S2 — National Geoportal** (official 753-municipality boundaries + web map; START-HERE #6), or curate governance headline charts / a governance orbit value (now that WGI is loaded). The orbit node still says governance "in preparation" — a small S6 follow-up. |
| 2026-07-25 | **P2B.S4 — headline-answer policy (decision 0005) + preferred sources wired** | With census + full WB both loaded, the same fact now has two numbers (population 29.16M census vs ~30M WB, etc.). Wrote **decision 0005**: one headline per fact — census for Nepali-counted facts (population/households/literacy), WB for internationally-modeled series (GDP/trade/inflation), NRB for banking/monetary; alternatives are **labelled, never hidden**. **Enumerated the real collisions** by matching census concepts to their WB twins — 6 census-vs-WB pairs (total/male/female population, growth, density, literacy); checked WB-vs-NRB and found **no true 1:1 collisions** (WB monetary aggregates are different constructs from NRB's BFS ratios — coexist, per decision 0003 §3); logged an intra-WB near-duplicate (`FS`/`FD.AST.PRVT.GD.ZS`) as unresolved for the founder. **A modelling problem surfaced and was fixed properly:** `preferred_source_id` was doing double duty as both provenance AND display-default, and the WB pipeline (P2B.S3b) scoped on it — so repointing a WB series' preferred source to the census would have silently stopped it being refreshed. Split the two: **migration 0006 adds `indicators.origin_source_id`** (immutable provenance, backfilled from preferred), the **WB pipeline now scopes on origin** (stable under repointing), and `preferred_source_id` becomes the genuine headline pointer. Headline repointing is seeded idempotently from **`db/seeds/headline_sources.csv`** via a new `seed_headline_sources()` that runs after the indicator seeders (which reset preferred→origin) — a missing code/source is reported, never guessed. **API:** `/v1/indicators` now returns each indicator's `source` (origin) and `preferred_source` (headline); when they differ the frontend badges "alternative estimate — {source}" (the P2B.S5 contract). Updated all three indicator seeders (WB/census/NRB) to set origin_source_id (now NOT NULL). | Applied to prod: `make migrate` (0006) + `make seed` → **"Headline sources repointed: 6"**. DB verified: the 6 WB pop/literacy series now origin=World Bank, preferred=National Statistics Office (alt=True); census headlines origin=preferred=NSO (alt=False); **WB pipeline scope on origin still exactly 1,356** (repointing preferred left ingestion untouched); 0 NULL origins; exactly 6 origin≠preferred. API `list_indicators` returns 1,396 rows in **184 ms**, flags the 6 alternatives correctly. `make lint` clean · `make test` **102 passed** (added an API badge test) · `cd web && npm run build` green. | **P2B.S5 — sector portal** (follow `docs/specs/frontend-sector-portal-spec.md` exactly; the badge fields are now in the API). Founder to approve deploy of this commit (backend-only: migration already applied to the live DB, so pushing the new API code keeps it in sync). |
| 2026-07-24 | **P2B.S3b follow-up — fixed the raw-archiving speed (3.8 h → seconds)** | The S3b load was correct but took ~3.8 h because `RawLake.store` did **three** sequential Supabase-Storage round trips per indicator (a redundant `exists()` pre-check + payload `put` + metadata sidecar `put`) — ~4,000 uploads at ~3.3 s each on the free tier. **Two fixes.** (1) **Dropped the `exists()` pre-check** in `store()`: the path carries a microsecond timestamp so it is unique per call, and `put()` already refuses to overwrite (`x-upsert:false` on Supabase, an explicit raise locally) — the pre-check was a wasted round trip on *every* write, for every pipeline. (2) **Added `RawLake.store_snapshot()`** and switched the WB pipeline to it: instead of one object per indicator, the whole catalogue is archived as **one immutable snapshot** (two uploads total, independent of member count). Each member's **untouched bytes are preserved verbatim (base64) with its own SHA-256 and source URL**, and the sidecar indexes every member's hash — so any single payload is still recoverable byte-for-byte and independently verifiable; the raw-first guarantee holds, only the object granularity changes. `fetch_all_series` is now fetch-only (no per-item archiving); the pipeline archives once after fetching, and if that single upload fails after retries it marks the log failed and loads nothing (raw-first). The number of Storage calls for a full run drops from ~4,000 to 2. | Real Supabase check: `store_snapshot` of 3 fetched payloads uploaded in **3.27 s** and every member round-tripped **byte-for-byte** (base64 + SHA-256 verified), test objects cleaned up. Full production run: fetched 1,356/1,356, **archived as one snapshot** (`worldbank/wdi-catalogue/…/snapshot.json` + `metadata.json` — now the whole of `releases.raw_file_refs`), **loaded 0** (idempotent, no regression), WB observations still **40,607** (no dup), quality gate PASSED, release 12 — the run was ~13–16 min, all of it WB fetch, archiving no longer a factor. `make lint` clean · `make test` **101 passed** (added a snapshot round-trip test). | **P2B.S4 — headline-answer policy** (WB population vs census now collide at scale; one designated headline number per concept, alternatives shown/labelled — decision 0005). The ~13-min WB fetch is inherent (sequential, deliberately not hammering the API) and fine for a scheduled refresh; leave it. |
| 2026-07-24 | **P2B.S3b — World Bank full mirror loaded into the warehouse (idempotent at scale)** | Picked up the uncommitted S3b work (pipeline rewritten for scale: three short-lived connections around a connectionless fetch phase, `executemany` batching, raw-first archiving with retry, `--dry-run`, stale-session + orphaned-log cleanup; `scripts/seed.py` loads the 1,336-code catalogue with verified metadata). **The `--dry-run` rehearsal earned its keep — it blocked the first load with 87 quality-gate failures, every one a *legitimate* WB value the gate wrongly rejected**, and I fixed the rule rather than the data (rule #1). The `PCT` band `[-10, 200]` was calibrated for the hand-curated 20 (shares like literacy/access) but the full catalogue puts three different quantities in that one unit: annual growth rates (`_ZG`, e.g. CO2 emissions +1497% off a tiny base), signed balances (external/current-account balance ~ −35% of GDP for Nepal's chronic trade deficit), and ratios of independent bases (reserves ~1116% of debt, tariff peaks ~918%). Kept the tight band on a named `BOUNDED_SHARE_CODES` set (10 hand-verified shares incl. `CENSUS_LITERACY_RATE`, so a 250% literacy rate still fails), and gave every other percentage a wide `[-1000, 5000]` **order-of-magnitude unit-misrouting guard** — its only job is to catch a count/level mislabelled `PCT` by the auto-catalogue, since a growth rate or ratio has no share-like bound to assert. Pinned with tests using the exact real values. **Second fix: the WB pipeline was not scoped to World Bank** — its indicator query `WHERE source_concept IS NOT NULL` also picked up the 35 NRB and 5 census indicators (their concept codes, e.g. `BFS.C4.ccdratio`, sent to the WB API — 40 wasted requests and one hard `Invalid value` error), and worse, would load a wrong value if a foreign concept ever collided with a real WDI code. Scoped it to `preferred_source_id = (this dataset's source)` with a note to revisit at P2B.S4. **The real load is correct but pathologically slow: ~3.8 h** — `RawLake.store` does 3 sequential Supabase-Storage round trips per indicator (exists + payload + metadata sidecar) = ~4,000 uploads on the free tier. It never hung (all HTTP calls have 30–60 s timeouts) and completed cleanly; flagged as the next thing to fix before scheduled refresh (P2B.S2) makes this a recurring 4-h job. | Load: **1,025 → 40,607 WB observations**, indicators-with-data **60 → 1,356**; sectors now non-empty — governance **0 → 15**, population **→ 89**, economy 667, health 185, education 154, labor 144, environment 110, agriculture 32. **Idempotency proven: a second `--dry-run` reports `would load: 0` (40,573 skipped).** **5/5 spot-checks exact vs the WB API** (GDP growth 4.43%, life expectancy 70.636, CO2 growth 1360.1%, external balance −24.9%, adult literacy 68.71 — the last two are formerly-blocked values, confirming the band fix). `make lint` clean · `make test` **100 passed** · `cd web && npm run build` green. `/v1/indicators` DB query **156 ms** for 1,396 rows (already indexed via `observations_unique_per_release`; no new index needed) — the 4–6 s end-to-end is Vercel cold-start + 179 KB payload, not the warehouse. | **Fix the raw-archiving speed before P2B.S4/S2 scheduling turns it into a 4-h recurring job** — batch the ~4,000 Storage uploads (combine per-run payloads, drop or inline the metadata sidecar, or archive concurrently). Then **P2B.S4 — headline-answer policy** (collisions now exist at scale: WB population vs census). Commit is ready but not pushed — founder to approve the deploy (push to `master` auto-redeploys API + site). |
| 2026-07-24 | **P2B.S3a — World Bank full catalog, curated and reviewed** (the founder STOP point: catalog reviewed, rules improved, then approved to commit) | An earlier session had produced `scripts/wb_catalog.py` + the two CSVs but left them uncommitted and unlogged. Reviewed the catalog at the STOP point and **found three problems worth fixing before loading anything.** **(1) The People sector was nearly empty (3 indicators) while Health absorbed ~70 age-band rows** — because WB publishes *no* "Population" topic: it files age structure under Health, the urban split under Climate Change and the rural split under Agriculture. Added a **demography override** keyed on the WDI code family (`SP.POP.` / `SP.RUR.` / `SP.URB.` / `SM.POP.` + a named list of birth/death/fertility rates and population density → `population`), with explicit exceptions so health outcomes carrying an `SP.DYN` code (life expectancy, infant mortality, contraception) stay in health and the two R&D-personnel codes keep WB's Science & Technology topic. Population **3 → 82**, health 216 → 183. **(2) The `governance` sector page was live with zero indicators** — the entire Worldwide Governance Indicators family ships with *no WB topic at all*, so all 121 untopiced indicators were being dropped to the unmapped report. Added a **code-family fallback used only when WB supplies no topic** (the first code segment is WB's own topic family, so this reads their classification off the code rather than guessing from the name): `GOV_WGI_*`/`GD_WBL_*` → governance, `IC`/`SI`/`DT`/`DC`/… → economy, HCI pillars split education/health/labor. Governance **0 → 15** (control of corruption, rule of law, government effectiveness, political stability, regulatory quality, voice & accountability — estimate + 0-100 score each). **(3) Rescued 41 indicators that had real units the rules simply lacked** — fertility (births per woman), pupil-teacher ratios, child working hours, cereal yield, precipitation, PM2.5, freshwater, alcohol, HIV headcounts, refugees — 15 new unit codes seeded in `db/seeds/units.csv`; also fixed `"current USD"` (WB's spelling, not `US$`) and deflators → INDEX. **Net: kept 1,213 → 1,336; unmapped 164 → 41 (11.9% → 3.0%).** **A bug I introduced and caught by auditing the output:** matching `"ratio"` as a bare substring also matches "mig**ratio**n", "agglome**ratio**ns" and "Ope**ratio**nal" — it had tagged a −364,699-person net-migration figure as a RATIO. Fixed to a whole-word regex and pinned with a regression test. **Deliberately left unmapped rather than guessed** (rule #1): 24 B-READY pillar scores (no unit stated anywhere in the name), the composite HCI (spans three pillars), SIPRI arms-import values, `1 in N` maternal-death risk, and two `.ZS` series — a "`.ZS` means percent" shortcut was tempting until `IC.FRM.NPRD.ZS` turned out to be `0.21`, not a percent. Added `tests/test_wb_catalog_rules.py` (46 cases) pinning every unit and sector rule. **Also fixed a broken verification gate:** `make lint` and `make test` were failing silently (exit 1, no output) because the `mypy.exe`/`pytest.exe` console-script shims in `.venv` hard-code an interpreter path that no longer resolves; the Makefile now invokes tools as `python -m <tool>`, which survives a moved venv. Unrelated to this step but it meant two of the three gates could not run. | `make lint` (ruff + mypy, 40 files) · `make test` (**94 passed**, 48 → 94 with the new rule tests) · `cd web && npm run build` — all three green. Catalog re-run is deterministic (identical summary on a second run). Every unit code in the catalog resolves against `db/seeds/units.csv`; no orphan units. Audited all RATIO/SCORE/COUNT rows by hand after the fix. | **P2B.S3b — load the 1,336 into the warehouse.** Before running `make ingest-wb`, confirm it batches with `executemany` on short-lived connections (Supabase drops sustained row-by-row writes at this scale); expect tens of thousands of observations; 5 spot-checks against data.worldbank.org; prove a re-run loads 0. Note the new sector counts will make the `/governance` and `/people` pages non-empty once loaded. |
| 2026-07-23 | **Landing credibility polish** (self-review as a senior data-product dev → fixed 3 P0 issues) | Reviewed the live landing critically and fixed the three things that read as "pretty but not credible": **(1) killed a same-screen contradiction** — the orbit showed People 29.2M (census) while a stat tile showed Population 29.65M (World Bank); reshuffled the 4 hero tiles to non-orbit macro metrics (Inflation, GDP per capita, Remittances %GDP, Internet users) so population now appears exactly once (orbit, census) — no contradiction and no orbit/tile duplication; **(2) labeled every orbit number** — added `orbitLabel` per sector (GDP growth, Avg lending rate, Population (2021), Life expectancy, Adult literacy, Unemployment, Electricity access), shown under each node's value and in the hover reveal, so a bare "70.6" now reads "Health · 70.6 · Life expectancy"; **(3) removed false precision** — hero/orbit values round to 1 decimal (3.7%, 6.7%, 94.0%) with full precision retained on the detail pages. | `npm run build` clean, `/` first-load **103 kB** (unchanged); resting + hover screenshots re-verified (`Screenshots/preview-home-fixed.png`, `preview-hover-fixed.png`). | Deploy; then P2B.S3a (WB full catalog) or founder's pick. |
| 2026-07-23 | **P2B.S6+ — premium interactive orbit** (founder ask: "something interactive, engaging, premium" on the landing page) | Enhanced `DataOrbit.tsx` into a guided-discovery hero (base orbit deployed first so the founder could see it live). **Hover/focus any sector node →** the Nepal center transitions to that sector's name + live value (`.orbit-caption` crossfade), a crimson dashed **connector line** draws from center to the node (SVG inside the rotor so it co-rotates and stays attached; pauses on hover), the active node lifts with a crimson ring, and the **other 7 nodes dim to 40%** to focus attention; the map softens (opacity+scale). **Numbers count up** 0→value on load via a rAF `OrbitValue` component (instant under `prefers-reduced-motion`). Keyboard-safe (onFocus/onBlur drive the same reveal; orbit already pauses on focus-within). Verified with a Playwright hover-state screenshot (People → center reads "PEOPLE 29.2M", connector + dim visible). | `npm run build` clean, `/` first-load **103 kB** (unchanged budget); hover screenshot captured (`Screenshots/preview-orbit-hover.png`). | Deploying; then continue the backlog — P2B.S3a (World Bank full catalog) or founder's pick. |
| 2026-07-23 | **P2B.S5 + P2B.S6 — sector portal + "Nepal data orbit" landing (founder-approved, deployed)** — built per `docs/specs/frontend-sector-portal-spec.md` | **The site is now organized by SECTOR (decision 0003) with a distinctive interactive landing page.** Founder chose to start with the frontend; picked the orbit; approved the design from a 3-breakpoint preview. **Built (both steps, one cohesive session):** `web/lib/sectors.ts` (the portal's shape as reviewable data — 8 sectors, membership rules incl. economy excludes `NRB_BFS_`, finance adopts them by prefix, every indicator lands in exactly one sector with a dev-console orphan/double guard; `sourceForCode` badges); `web/lib/latest.ts` (shared latest-observation helper extracted from HeroStats — prefers the aggregate bank class AND the census **total** row, fixing an orbit bug where People showed a single sex's 14.9M instead of 29.2M); **sector pages** `web/app/[sector]/page.tsx` (8 static slugs, `dynamicParams=false`, per-sector metadata) → `SectorDashboard` (crumb/title/meta, "At a glance" headline band, full source-grouped indicator list with filter + unit chips + source badges, governance empty-state) + `HeadlineChart` (lazy-loaded TrendChart so ECharts stays out of first-load; breakdown-slicing to the headline series); **nav** "Data ▾" native `<details>` dropdown of the 8 sectors; **deep links** `/explore?indicator=` + `/banking?indicator=` (client-side param read — keeps both routes static, no Suspense needed); **the orbit** `DataOrbit.tsx` — Nepal silhouette (dissolved from the **2020** provinces map, ~4.5 kB inline path), 8 sector nodes on a precomputed ellipse, CSS-only 150s rotor/counter-rotor (upright text), hover/focus pause, `prefers-reduced-motion` off, <560px reflow to a static 2×4 grid, live orbit numbers via one `Promise.allSettled`; real `<Link>` nodes (keyboard/SR accessible), ring+center `aria-hidden`; landing hero → 2-col grid, `SectorCards` (8 cards + minimal line icons + live counts). TrendChart gained `height`/`compact` props. **A11y note:** chose real accessible links + focus-pause over the spec's aria-hidden+sr-only-nav (simpler and more correct; DOM order = sector order). | `cd web && npm run build`: **all 15 routes static** (8 sector pages SSG), first-load `/` **103 kB** (≤110 budget; no chart JS), `/[sector]` **102 kB** (≤120); TS strict clean. Playwright screenshots captured at 360/768/1440 for `/` and `/economy` (real live-API data) → founder reviewed a preview artifact and approved. | **Deploying now** (founder wants it live). Then founder asked for **more premium interactivity on the landing page** — building an interactive orbit enhancement next (hover/focus reveals the sector's stat in the center + connector, staggered entrance, count-up). CI Playwright e2e job (spec §7.4) deferred to a follow-up. |
| 2026-07-23 | **P2B.S2 — scheduled ingestion + `/v1/meta` freshness (automation before expansion)** — code complete & verified; founder must add GitHub secrets to activate | **The pipelines can now run themselves and the site can say when data was last updated** — the staleness fix every later source inherits. **(a) `.github/workflows/ingest.yml`:** two cron jobs + `workflow_dispatch` (manual "Run workflow" with a job picker). **world-bank** weekly (Sundays 03:00 UTC) → `make ingest-wb`; **nrb-bfs** monthly (the 20th) → `acquire → extract → status`, and **never promotes** (human-made Excel stays behind the review gate, rule 3) — it parses the extract output for "N staging rows written" and posts a review-and-`make nrb-bfs-promote`-locally notice to the job summary. Census gets no schedule (decennial). A non-zero exit fails the job red → GitHub emails the founder = the staleness alarm. Runs the make targets with **`PY=python`** so the venv-path Makefile works on the runner (command-line var override). Job-level `env` wires 4 secrets. **(b) `GET /v1/meta`** (API): per-dataset latest **successful** `ingestion_log.finished_at` + latest `release_date` via a `FILTER (WHERE status='success')` group-by (only datasets that ever loaded appear); top-level `data_updated` = newest of them. New `DatasetMetaRow`/`get_meta` on the repo Protocol + Postgres impl; `DatasetMeta`/`MetaResponse` models; offline test with the fake repo (+1 → **48 pass**). **(c) Footer freshness:** `DataFreshness.tsx` client component fetches `/v1/meta`, renders "Data last updated {DD Month YYYY}" (crimson date), **fails silently** if the API blips (freshness is nice-to-have); wired into `SiteFooter` + a small style. **(d)** keep-alive.yml comment + README updated (secrets list, the 60-day cron-disable rule that any push resets, manual-run instructions). **Verified `/v1/meta` against the REAL warehouse:** 3 datasets — Census 2021 (2026-07-19), NRB BFS (2026-07-10), WB WDI (2026-06-14); footer date resolves to **19 July 2026**. | `make lint` clean · `make test` **48 passed** (+1) · `cd web && npm run build` clean (routes still 92–99 kB, static); `ingest.yml` valid YAML (jobs world-bank/nrb-bfs; schedule+workflow_dispatch); live `get_meta()` query returned real per-dataset dates. Committed locally; **not yet pushed** (deploys the footer + `/v1/meta`). | **Founder actions to close the step:** (1) approve the push (deploys `/v1/meta` + footer date); (2) add 4 **Actions secrets** (`DATABASE_URL` pooler, `SUPABASE_URL`, `SUPABASE_SERVICE_KEY`, `STORAGE_BUCKET`) — I'll give click-by-click; (3) Actions tab → "Run workflow" on each once to confirm green + one forced-failure test. Then next backlog step: **P2B.S3a — World Bank full catalog** (~1,400 indicators; has a founder STOP to review the catalog summary). |
| 2026-07-22 | **P2B.S1 — the official 2020 map of Nepal is LIVE (Limpiyadhura/Lipulekh/Kalapani) + decision 0004** (first BUILD after the 7-script planning stretch; foundation step 1 of START-HERE) | **The portal now renders Nepal's correct post-2020 political border** — the single most visible correctness fix on the site. **Problem re-confirmed:** our shipped `web/public/maps/nepal-districts.json` was 2017-vintage — Darchula (verified P-code **NP0775**, not the step's guessed NP0709) topped out at **30.247°N**, short of Limpiyadhura (~30.44°N). **Source found (best possible):** the **Survey Department's own** *"New political and administrative boundaries"* shapefile, republished via **Open Data Nepal** (CKAN org = Survey Department), license **CC-BY-SA** — official authority + open license + accessible, all at once. Pulled the raw ZIP via the CKAN resource API (777 local-unit polygons, Everest-1937 datum). **Derivation (mapshaper, reproducible in PROVENANCE):** `-proj wgs84` → dissolve to 77 districts / 7 provinces / national outline; rewrote properties to the portal's **exact existing schema** (`DIST_EN/DIST_PCODE/ADM1_PCODE`; `ADM1_EN/ADM1_PCODE`) so the swap is invisible to the app (map join is pure P-code attribute, `ChoroplethMap.tsx`). **73/77 district names auto-joined; the 4 split districts** (Nawalparasi E/W, Rukum E/W) mapped **explicitly and disambiguated by province code** (fail-loud if `ADM1_PCODE≠NP0{STATE_CODE}`) — never guessed. Simplified districts 4% / provinces 8% `keep-shapes`. **4 acceptance checks all pass:** Darchula now reaches **30.473°N** (includes Limpiyadhura); national area **148,261 km²** = **0.50%** off the official 147,516 (and *new−old* = +391 km² matches the official +335 km² territory gain — independent confirmation the wedge is real); all 77 P-codes present & unique + NP01–07; sizes 307/232 kB (≤350). Old files **archived, not deleted** (`reference/geo/pre-2020-archive/`). Heavy raw/work git-ignored (re-fetchable from the URL in PROVENANCE — repo stays lean per norm; raw lives outside git like the raw lake). Wrote `docs/decisions/0004-official-map.md` (boundaries verify-against-official, never traced; CC-BY-SA attribution) + a dependency-free before/after SVG (`reference/geo/official-2020/darchula-2020-map-fix.svg`). | Darchula max-lat 30.2467→30.4731; area 0.50% of official; 77/7 P-codes intact; `make lint` clean · `make test` **47 passed** · `cd web && npm run build` clean (`/population` 92.3 kB, static). Founder reviewed a self-contained before/after artifact (real geometry, old-vs-new toggle) and approved → **pushed & DEPLOYED** (commit 3454245). **Live verified:** `/population` HTTP 200; the deployed `/maps/nepal-districts.json` reports Darchula max-lat **30.4731** with 77 features → the 2020 border is live on the public site. | **P2B.S1 complete & live.** Next backlog step: **P2B.S2 — scheduled ingestion + /v1/meta freshness** (GitHub Actions cron + footer "last updated"; everything later inherits it). |
| 2026-07-19 | **CEHRD education recon + onboarding step file** (paste-a-link workflow, 6th run; also: edusanjal.com declined) | Founder asked about edusanjal.com → **declined with reasons** (private company, "All rights reserved", a listings/marketing directory with no statistics — ingesting it would breach copyright and the portal's trust ethos); recommended the official channel instead; founder then supplied `cehrdservices.moest.gov.np` → that subdomain is **unreachable externally** (instant connection failure; likely internal — recorded, re-probe in EDU.S2), but recon pivoted to **`cehrd.gov.np`, which works and is the real prize** → **`docs/steps/onboard-cehrd-education.md`** (4 steps). **Findings:** CEHRD runs the **same GIWMS/Prixa CMS as MoF** — the `var pdf` CDN pattern verified live on **Flash 1 Report 2081 (2024)** (content/14179 → giwmscdnone.gov.np PDF) ⇒ EDU.S1 REFACTORS the MoF acquirer into a shared `giwms.py` harvester (two sources, one module, test-locked); nav exposes flash-report/status-report/research/publications/downloads/statistics categories (older Flash years hunted across all of them, coverage reported not assumed); **IEMIS live at emis.cehrd.gov.np** (school-level MIS — EDU.S2 spikes for public endpoints, never past a login wall). **Modeling pre-made:** EDU_SCHOOLS/STUDENTS/TEACHERS/GER/NER/STR with level+sex breakdowns, district rows joined to P-codes (alias precedent), **school-year period mapping verified from the report's own definition** (academic_year period_type added only if it genuinely differs), PDF ⇒ staging+review, spot-checks vs printed PDFs + cross-checks vs the ODN Ministry-of-Education datasets (independent, cc-by). EDU.S4: education dashboard with NER/GER district choropleths, census-literacy vs Flash-enrollment kept as distinct labeled concepts (decision-0005 discipline). | Live probes: cehrd.gov.np 200 (GIWMS confirmed), Flash PDF URL extracted, emis 200, services subdomain 000; step file committed. | 7th and final script of the day. EDU.S1 rides the shared harvester; CEHRD joins the scheduled monthly acquire. UGC (higher ed) noted as the next paste-a-link candidate. |
| 2026-07-19 | **The Budget Center — three-tier budget product designed + scripted** (founder's product vision) | Founder proposed a dedicated Budget section tracking **federal + province + local** budgets as a visually striking dashboard ("the data everyone is looking for") → recon + **`docs/steps/budget-center.md`** (4 steps + full dashboard design spec). **The unlock found in recon:** the local tier does NOT require scraping 753 municipal sites — MoF's annual **अन्तर सरकारी वित्तीय हस्तान्तरण (Intergovernmental Fiscal Transfer)** annex publishes federal transfers per province AND **per local government** by grant type (equalization/conditional/complementary/special/revenue-share); both FY 2082/83 (content/1526) and FY 2083/84 (content/1751) verified on the site, alongside Appropriation Acts, English Budget Speeches (2025/26, 2026/27) and the Red Book volumes (to be located via the MOF.S1 index — the CMS category feeds are mixed). **Steps:** BUD.S1 transfers per province+LG (PDF tables, staged+reviewed, LG names joined via the census municipality extraction with fail-loud curation, sums vs printed totals), BUD.S2 federal totals/composition/ministry allocations (English speech annex first, Red Book summary table only, WB-dashboard cross-checks), BUD.S3 province budgets (WBF series + 7-province site recon memo, honest coverage labeling), BUD.S4 the `/budget` dashboard — design LOCKED: hero number + single stacked composition bar (no pie), top-10 ministry bars, province choropleth with **per-capita toggle (÷ census population — the differentiator)**, "Find your palika" search over 753 LGs with per-LG grant breakdowns, bilingual labels, glossary from MoF's own grant procedures, budget-season annual refresh riding MOF.S1's scheduled harvest. **Honesty note recorded:** local tier v1 = transfers TO municipalities (what's published), not full LG budgets; SuTRA/FCGO logged as a future open question, not promised. Dependencies wired: MOF.S1 → WBF.S1–2 → NGP.S2/P2B.S8 → BUD. | Live listing probes of the redbook + IGFT categories (both IGFT fiscal years present with content ids); step file committed with the design spec and cross-source integration map. | 6th script on the shelf. Recommended entry after MOF.S1 + WBF.S2. Founder-visible milestone: BUD.S4 with tiers 1+2+transfers live. |
| 2026-07-19 | **MoF publications recon + onboarding step file** (paste-a-link workflow, 5th run) + nepalindata.com finding | Founder supplied mof.gov.np/category/bulletin → recon → **`docs/steps/onboard-mof-publications.md`** (4 steps). **Findings:** GIWMS/Prixa CMS; every publication's PDF is embedded in its content page as `var pdf = 'https://giwmscdnone.gov.np/media/pdf_upload/…'` (pattern verified on the FY 2081/82 annual bulletin); the library spans **अर्थ बुलेटिन (periodic economic bulletin), Economic Survey, Budget Speeches (English editions exist), Red Book, White/Yellow Books, inter-governmental fiscal transfers, half-yearly budget assessments** — the PRIMARY documents behind the WB fiscal dashboard. **Plan is "library first, tables second"** (PDF mining is our most expensive channel): S1 full raw mirror + machine-readable index with idempotency manifest (storage-budget STOP at ~400 MB); S4 founder-visible **bilingual publications-library page** (links out to originals — no parsing needed); S2 Artha Bulletin stable-table parsing (NRB-style stability recon first; curated Nepali→English `label_map.csv` reviewed by a human — the founder's "translate" ask, done per reference-data rules; lakh/crore/arba conversions encoded with tests; staging+review; cross-check vs WB dashboard with differences REPORTED not forced); S3 Economic Survey annex — Excel-first check, PDF-parsing decision memo otherwise. **Translation policy recorded:** labels/titles YES via human-approved mapping CSVs; prose NO (link to originals). **Also this session: nepalindata.com checked — the former best-known Nepal data portal is now a brochure site with NO data (all data paths 404)** — no step file warranted; competitive validation + cautionary tale logged. | Live probes: category listing parsed (50+ items incl. Budget Speech EN 2026/27, Economic Survey 2082/83); `var pdf` CDN pattern extracted from a real content page; nepalindata 404s verified. | 5th source script on the shelf (ODN, WB fiscal, geoportal, ECN, MoF). MOF.S1→S4 recommended before S2. `mof-acquire` joins the P2B.S2 monthly schedule when built. |
| 2026-07-19 | **Election Commission recon + onboarding step file** (paste-a-link workflow, 4th run) | Founder supplied election.gov.np → recon → **`docs/steps/onboard-election-commission.md`** (4 steps) — fills the **empty Governance sector**. **Findings:** main site = React SPA over `election.gov.np/admin/public/api/` (site content only); the data lives at **`result.election.gov.np`** — an ASP.NET/JQGrid results portal currently serving the **2082 BS House of Representatives election** (`PRVoteChartResult2082.aspx`, `MapElectionResult2082.aspx`; earlier cycles 2079/2074 expected behind the same portal — S1 enumerates); `voterlist.election.gov.np` exists → **binding privacy boundary written in: individual voter data is NEVER ingested, aggregates only**. **Sensitivity policy baked in:** final published results only (no live-count scraping), neutral presentation, no party brand colors (portal palette + top-8+Other per dataviz series cap), ECN attribution everywhere. **Modeling pre-made:** ELECTION_VOTERS_REGISTERED / TURNOUT_PCT (district choropleth via existing machinery), ELECTION_VOTES_PR + ELECTION_SEATS with `{"party"}` breakdowns (names stored exactly as published, Devanagari primary, curated transliteration CSV), election-year periods with exact dates in definitions, cross-checks (party votes ≤ registered voters; seats sum to 275; national totals vs ECN press releases). S1 is a fail-safe spike incl. the Playwright-observe-then-replay technique for dynamic XHR endpoints and a PDF-only fallback STOP. Recon note: the ECN pages are Devanagari-heavy — the cp1252 stdout crash resurfaced during recon itself; UTF-8 rule restated in the file. | Live probes: subdomains 200; API base extracted from the React bundle; 2082 result pages confirmed on the results portal; step file committed. | 4th source script on the shelf (ODN, WB fiscal, geoportal, ECN). Implement via "We are on step ECN.S1 of docs/steps/onboard-election-commission.md". |
| 2026-07-19 | **National Geoportal recon + onboarding step file** (paste-a-link workflow, 3rd run) | Founder supplied nationalgeoportal.gov.np → recon → **`docs/steps/onboard-nationalgeoportal.md`** (3 steps). **Findings:** the Survey Department's portal runs an open **GeoServer WFS** (`/geoserver/ows`, unauthenticated, GeoJSON output) with **82 layers** incl. official admin boundaries at every level — notably **`base_nepal_local_level_new` (the 753 local units)**, wards, districts, provinces, plus health facilities, rivers, peaks, roads. **Two decisive tests run live (ray-cast point-in-polygon, Kalapani test point 80.75E/30.30N + control):** (1) **our deployed districts file is confirmed PRE-2020** (Darchula max lat 30.247, Kalapani point outside) — the founder's map concern is now proven fact, not suspicion; (2) **the geoportal itself still serves the pre-2020 international boundary in every national layer** — its "new"/"old" naming refers to the 2015 federal restructure, not the 2020 political map. P2B.S1 updated in place: geoportal channel checked-and-ruled-out, verification half of the step already done; remaining channels start at OCHA/HDX COD-AB post-2020. **Value captured anyway:** NGP.S1 harvests + reconciles the official boundary set against our P-codes; NGP.S2 produces the official 753-local-unit web map (unblocks P2B.S8, replacing the MIT-mirror file); NGP.S3 (optional) turns the health-facilities layer into a per-district choropleth indicator. Authority top-tier (government's own channel); no license text published — recorded honestly. | Live WFS GetCapabilities (82 layers listed); national outline fetched (1 feature, 1.7 MB) and tested; our Darchula bbox computed from the deployed file; step file + P2B.S1 update committed. | NGP.S1→S2 before P2B.S8; the 2020-boundary hunt proceeds via P2B.S1's remaining channels; founder now has 3 source scripts (ODN, WB fiscal, geoportal) + the P2B roadmap + frontend spec, all implementable by any model. |
| 2026-07-19 | **WB Nepal Fiscal Dashboard recon + onboarding step file** (paste-a-link workflow, 2nd run) | Founder supplied the World Bank Nepal Fiscal Dashboard link → recon → **`docs/steps/onboard-wb-nepal-fiscal.md`** (4 steps). **Findings:** a **Tableau workbook** (`dataviz.worldbank.org/views/NepalFiscalDashboard`, embed API v3) carrying **federal + provincial budgets/revenue/expenditure FY2018–FY2024**, compiled by WB from MoF budget speeches, Provincial MoFs, **FCGO consolidated financial statements**, and NRB; last updated 2026-02-12; page itself notes sub-national coverage caveats (reproduced as a UI note in the plan). **Why prioritized:** public finance is an empty portal sector; per-province fiscal data is near-unique; it lands on machinery we already have (BS fiscal-year periods + NP01–NP07). **Access reality:** naive `.csv` endpoint returns a shell — requires the **Tableau session protocol** (GET view → `X-Session-Id` → `bootstrapSession` → per-sheet data endpoints), our most brittle acquisition yet; step file encodes the exact protocol, three ordered channels (session harvest → WB Data Catalog / download permissions → STOP with email-WB / FCGO-primary fallback memo), politeness rules, and never-scrape-pixels. **Modeling pre-made:** indicators per fiscal concept (not per sheet), category detail in `breakdowns`, budget-vs-actual as separate indicators, unit READ from harvested labels (NPR millions vs billions — the classic 1000× trap called out), FY-labeling convention verified before BS mapping, scraped ⇒ staging+review gate, spot-checks against MoF Red Book figures (independent channel). Frontend lands as a Public-finance panel (Economy sector) with province comparison via the existing choropleth. Refresh = a tiny "Last Updated" watcher, no cron ingestion. | Step file committed; provenance policy mirrors ODN (WB = compiler, MoF/FCGO/NRB = authorities); raw-lake-first protects against protocol breakage after first harvest. | Implement via: "We are on step WBF.S1 of docs/steps/onboard-wb-nepal-fiscal.md" — S1 is deliberately a cheap spike that fails safe. |
| 2026-07-19 | **Open Data Nepal recon + onboarding step file** (first run of the "founder pastes a link" workflow) | Founder supplied opendatanepal.com → live recon → **`docs/steps/onboard-opendatanepal.md`** (5 steps, ~5–7 sessions, implementable by any model). **Findings:** Next.js frontend over a **CKAN API at `api.opendatanepal.com/api/3/action`** (base found in the site JS); **382 datasets / 25 orgs**, top publishers CBS (91), DoHS (39), Agriculture (12), DHM (9); licenses mostly cc-by (287) + cc-by-sa (39), 50 `notspecified` (quarantined pending founder sign-off); 377/382 CSV; **CKAN datastore ACTIVE** → clean paged JSON rows, no CSV scraping; actively maintained (modified 2026-06-15). **Policy baked into the file:** ODN is an AGGREGATOR — originating agency = `sources` row, ODN = distribution channel recorded on the dataset; per-dataset authority + license check mandatory; historical snapshots labeled with their window, never presented as current. **Curated first targets:** T1 **Kalimati daily fruit/veg prices 2013→present** (flagship; verified sample row Tomato Big (Nepali) 2013-06-16 min 35 / max 40 / avg 37.5; modeling decided: ONE indicator per stat with `breakdowns={"commodity"}`, top-25 basket, day periods on demand, Min≤Avg≤Max gate, staging+review, ~250–350k rows with a DB-headroom STOP), T2 tourist arrivals 1992–2014 (closed series, cross-checked vs annual set), T3 DHM climate normals/trends (station breakdowns, no fake per-year rows), T4 provincial health FY 2073/74 (BS fiscal periods, province-number→P-code only), plus S5 = full 382-dataset triage report. ODN.S1 builds a **reusable CKAN client** — many gov portals are CKAN, so this is the P2B.S11 template's first dividend. | Step file committed; facts bank pins API base, org slugs, resource id + verified sample record; all curation decisions pre-made with STOPs where founder input is needed. | Implement via: "We are on step ODN.S1 of docs/steps/onboard-opendatanepal.md." Kalimati weekly refresh folds into P2B.S2's scheduled workflow. |
| 2026-07-19 | **Frontend implementation spec authored (P2B.S5+S6 detail)** | Founder chose to bank Fable 5's design judgment as a spec rather than burn the remaining budget building: wrote **`docs/specs/frontend-sector-portal-spec.md`** — an implementation-grade companion to P2B.S5/S6 that a different model (Opus, Sol, …) can execute without making any design decision. Contains: full `sectors.ts` data (8 sectors, membership carve rules incl. `NRB_BFS_` → Finance, curated headline codes per sector from today's real 60, orbit codes, map-card links, verbatim titles/descriptions); nav spec (native `<details>` "Data ▾" dropdown); dashboard-style sector page layout with a `HeadlineChart` contract incl. the breakdown-filtering algorithm (bfi_class overall→commercial fallback; sex→empty-breakdown rows); `/explore` + `/banking` `?indicator=` deep links with the `useSearchParams`+Suspense build gotcha; the **orbit hero** fully specified (mapshaper dissolve command for the Nepal silhouette path, ellipse node placement `θ=-90°+i·45°`, rx 46%/ry 42%, 150s rotor/counter-rotor CSS with hover-pause + reduced-motion off, <560px static-grid fallback, `aria-hidden` visual + sr-only nav, zero new deps, ≤110 kB `/` budget); Playwright smoke + screenshot protocol (360/768/1440 into `Screenshots/` for the founder's go/no-go — the P2B.S6 STOP); CI e2e job; budgets; the orbit's fallback path if founder vetoes; and a **"known traps"** list (eager chart imports dragging ECharts into first-load, Suspense build failure, commercial-only NRB breakdowns, cold serverless first-hit, cp1252 console). Step file now points to the spec with a paste-ready kickoff prompt. | Spec committed; P2B.S5/S6 remain the acceptance gates; kickoff prompt embedded in the step file. | Founder hands the kickoff prompt to the implementing model; the P2B build order otherwise unchanged (S1 map first). |
| 2026-07-19 | **Phase 2B step file authored — the expansion plan, judgment front-loaded** | **Founder session (brainstorm → plan, deliberately no build): audited the portal's data honestly, then wrote `docs/steps/phase-2b-expansion-steps.md` — 11 steps (~16–20 sessions) that any capable model can implement mechanically, with today's recon and judgment baked in.** Founder decisions recorded in the file: portal reorganizes by **sector** (re-affirming decision 0003 — the current site had drifted source-shaped); sector pages **dashboard-style**; 8 sectors locked (Economy, Finance & Banking, People & Population, Health, Education, Labor, Agriculture & Environment, Governance); landing page pursues a **"Nepal data orbit"** concept (accessible, ≤40 kB, founder go/no-go on screenshots before deploy); implementing model works autonomously with explicit STOP points only (WB catalog review, orbit go/no-go, boundary dead-end, per-bank decision). The steps: **S1 official 2020 map** (Limpiyadhura/Lipulekh — current boundary files are 2017-vintage, near-certainly pre-2020; verify-then-replace protocol, never trace; decision 0004) · **S2 scheduled ingestion** (GitHub Actions cron + `/v1/meta` freshness; NRB promote stays human-gated) · **S3 WB full mirror** (~1,400 indicators; exact unit-inference and WB-topic→8-sector mapping rules written; unmapped → report, never guessed) · **S4 headline-answer policy** (census vs WB collisions; decision 0005; `preferred_source_id` finally used) · **S5 sector portal** (absorbs P3.S2; Playwright smoke lands here) · **S6 landing page** · **S7 census all topics** in 5 parser families (endpoint inventory from the verified facts bank; sensitivity rule for religion/caste/language layers: neutral presentation, restraint by default) · **S8 municipality level** (753 units; the id list extraction is documented; drill-down UI by actual availability) · **S9 NRB C5–C7** (stability report across all 59 archived files BEFORE any parser; units verified from the sheets) · **S10 per-bank scoping memo** (decision 0006 draft; no code) · **S11 source-recon template** (founder pastes any URL → recon session → ready step file). Reconciliation section marks P2.S11 superseded, P2.S4 done for 7+77, P3.S1 partial, P3.S2 absorbed; pointer notes added to phase-2 and phase-3 files. A "facts bank" section pins the verified constants (census API base, id schemes, spot-check values, WB enumeration URLs, Supabase batching rules) so future sessions cite instead of re-deriving. | File committed with cross-references; contains explicit verification checklists, IF-IT-GOES-WRONG paths, and commit messages per step; standing rules footer (gates green, raw-first, idempotent, report-never-guess, log every session). | Founder says "build" per step — recommended order S1 → S2 → S3 → S4 → S5 → S6, then S7+ can interleave with Phase 3. Open founder items: eyeball /population on phone; hero polish folds into S6. |
| 2026-07-19 | **Census 2021 onboarded + /population map dashboard LIVE** (founder priority: NSO data, easier than the official dashboard, GIS map, easy download) | **The portal now paints Census 2021 on an interactive map of Nepal — province and district choropleths with hover figures, ranked table, and CSV download — from our own warehouse.** **Recon (never guessed):** the official results site (censusresults.nsonepal.gov.np, Next.js) runs on a **public JSON API — `censusapi.cbs.gov.np/api/v1`** (found as `baseURL` in its JS); the 77-district id scheme (ids 1–77, provinces 1–7) is compiled into the site's frontend, extracted verbatim; district-per-province counts match the official 14/8/13/11/12/10/9; **all names (EN + Devanagari) taken from NSO's own locale files** (`/locales/{en,np}/common.json`) — provinces Koshi…Sudurpaschim / कोशी…सुदुरपश्चिम. **Geography master:** +7 provinces +77 districts into `geographies` via the existing seed loader — codes = **OCHA P-codes** (NP03, NP0327…) so warehouse rows join the map files directly; 74/77 district names auto-matched to the MIT-licensed GeoJSON (mesaugat/geoJSON-Nepal), 3 spelling variants (dhanusa/kavrepalanchok/makwanpur) mapped explicitly after uniqueness+province checks; chain verified: NP0327 Kathmandu (काठमाडौँ) → Bagmati (बागमती) → Nepal; idempotent (85 rows stable). Raw evidence archived under `reference/census/` with PROVENANCE.md. **Pipeline** (`ingestion/nso/`): pure `census_layout` parsers (registry⇄CSV locked by test; unknown shapes raise) + `census_pipeline` — raw lake BEFORE parsing (170 objects/run), one release, census-specific quality-gate bands, change-aware per (indicator, geo, period, breakdowns), batched `executemany`. **Loaded 765 obs** (5 indicators × 85 geos; sex breakdowns for population + literacy) on period 2021, status=final; **re-run: 0 loaded / 765 unchanged**. **API:** new `GET /v1/data/geo?indicator=&level=` returns every geography's latest value + provenance in one call (+3 tests → 47). **Web:** `/population` — ECharts choropleth (modular MapChart+VisualMap in the lazy chunk), sequential blue ramp, Devanagari in tooltips/table, pan/zoom, province/district toggle, CSV; boundaries simplified 14MB→279kB & 5.4MB→209kB via mapshaper; landing card + nav updated; route 92.3kB first-load, static. | Spot-checks vs published figures: national 29,164,578 exact; **Kathmandu district 2,041,587 exact**; 7 provinces **sum exactly** to the national total; density extremes Kathmandu 5,169 / Manang 3 per km²; literacy range Rautahat 57.8 → Kathmandu 89.2. `make lint` clean; `make test` **47 passed** (35→+12); `npm run build` clean; **live on the public internet** (commit `1afb187`): API geo endpoint 200 with correct sums, `/population` 200, map assets 200. | **Founder eyeball:** open `/population` — map paints, hover works, both levels, phone. Then: more census topics (households, age structure, mother tongue — same pipeline pattern), municipality level (753 units, list already extracted), hero-section polish (founder noted), Playwright smoke test, custom domain + Render shutdown decision. |
| 2026-07-18 | **P3 visual pass — premium redesign live; first-load JS cut 4.4×** | **The public site was redesigned end to end into an MVP the founder can showcase** (founder directive: "best UI/UX and performance, professional and premium"; geography crosswalk deliberately deferred). Direction: data-journalism — warm paper page, white cards, deep navy ink, single Nepal-crimson accent, Fraunces display serif + Inter (self-hosted via next/font, zero external requests). **(a) New landing page `/`:** hero + mission, 4 live headline stat tiles (GDP growth, CPI, population, avg lending rate — client-fetched with shimmer skeletons; NO chart JS on this route), sector cards (Explore / Banking / "Population & census — in preparation"), trust strip (source-first, nothing invented, traceable), inline ridgeline SVG. Indicator explorer moved to **`/explore`**. **(b) Dashboards rebuilt:** server pages for per-route metadata + client dashboards; summary row (Latest / Change / Coverage), **CSV download**, table view, segmented Levels/MoM/YoY control, bank-class chips; §3.6 error states kept. **(c) Performance:** replaced `echarts-for-react` + full echarts import with a **modular echarts/core build** (LineChart+Grid+Tooltip+Legend+Canvas only) behind one `EChart` wrapper; palette split into chart-free `lib/bankingPalette.ts` after the first build showed a static import dragging echarts into the eager bundle (267 kB → 93 kB). **First-load JS: 434 kB → 92–99 kB per route, all routes static.** **(d) Chart colors machine-validated** per the dataviz method (six-checks validator): banking categorical order green/blue/amber/violet on white passes ALL checks (worst adjacent CVD ΔE 26.5; ≥3:1 contrast); single-series = brand crimson; order documented as the CVD-safety mechanism. Site is light-only by design (dark mode = a later deliberate step). | `npm run build` clean (strict TS), routes `/` 98.8 kB, `/explore` 92 kB, `/banking` 93.1 kB first-load, all static; local `next start` smoke: 3 routes 200, hero/sector-cards/4 skeleton tiles in SSR HTML, fonts emitted to `.next/static/media`; live after deploy (commit `a8d5aa3`): 3 routes 200 on the public URL, API base confirmed inlined in the live JS. Palette validator output in-session. | **Founder eyeball:** open the live site — charts painting, hover, mobile width (no headless browser here). Then: Census 2021 province data + map (needs 7-province seed + working local DATABASE_URL), more sources, Playwright smoke test (P1 lesson e), consider custom domain + shutting down Render. |
| 2026-07-18 | **Portal migrated to Vercel — website + API now LIVE with no cold-start sleep** | **The public portal is now fully live on Vercel's free (Hobby) plan, replacing Render as the shared link.** *Why the move:* Render's free tier sleeps after ~15 min idle (measured ~63s API / ~24s web first-hit), making a shared link look broken. Vercel Hobby does not sleep and **cannot bill** (no card; over-limit pauses, not charges) — decisive for a non-technical solo founder. A prior session (commit `cc5eef0`, 2026-07-16) had already added the API-on-Vercel config (`vercel.json`, `api/index.py`, `requirements.txt`) but was never logged and never actually deployed. This session took it live end to end. **Debug chain (never guessed — verified each):** **(1)** Founder created the Vercel account, imported the repo, deployed the **API** (`nepal-data-base-project.vercel.app`) — `/health` 200 but data endpoints **500**. **(2)** Checked Supabase itself: `check-db` failed to resolve `db.<ref>.supabase.co`, REST endpoint dead, direct host NXDOMAIN while the shared pooler host still resolved → **the dev DB had auto-paused** (the free-tier ~2–3 wk idle pause again). Founder restored it from the dashboard. **(3)** API still 500'd after restore → root cause = the imported local `.env` `DATABASE_URL` uses the **direct IPv6-only host**, which Vercel (like Render) cannot reach. Founder copied the **working pooler DSN from Render's env** into Vercel's `DATABASE_URL` + redeployed → API served real data (55 indicators, GDP 64 pts 2024=3.67, NRB 59 pts). **(4)** Website: created as a 2nd Vercel project from the same repo; first build failed `No Output Directory named "public"` because it built from repo root, not `web/`. Set **Root Directory=`web`** + Framework=Next.js; "Redeploy" still failed (reuses the old build's mis-detected settings). Fixed by **pushing a commit** (`5667ed3`, added `web/vercel.json` pinning `framework:nextjs`) to force a fresh build → site live at **`nepal-data-base-project-7oru.vercel.app`**, title + `/banking` 200, API base correctly inlined into the JS bundle. **(5)** Site loaded but charts errored "Couldn't reach the data service" → **CORS**: API had no `Access-Control-Allow-Origin` (defaults to localhost only). Founder set `CORS_ALLOW_ORIGINS=*` on the API project; empty-commit push (`87ff212`) rebuilt it → header now `*`. **Founder confirmed charts render in the browser.** Also added **`.github/workflows/keep-alive.yml`** (daily API ping so Supabase can't auto-pause + free uptime alert) and updated README/CLAUDE.md to make Vercel the documented home (Render kept as fallback). Memory: new `vercel-deploy`; `screenshot-folder-convention` (founder drops UI screenshots into `Screenshots/`). | Public internet, verified: website `/`=200 (title "Nepal Data Portal") + `/banking`=200; API-base baked into `/_next/...page-*.js`; API `/v1/indicators`=55, GDP_GROWTH 64 pts, NRB_BFS_LENDING_RATE 59 pts; API CORS header `Access-Control-Allow-Origin: *` for the site origin. Founder eyeballed the charts painting. | **Optional polish:** prettier URL (rename project or custom domain); decide whether to shut Render down now Vercel is primary. Confirm the keep-alive cron runs (Actions tab) and that `CORS_ALLOW_ORIGINS`/`DATABASE_URL` are marked for Production in Vercel. Then **back to data work:** extend NRB BFS beyond C4 (C5–C7 levels, per-bank tables) + 2011–2021 backfill; Playwright smoke test for canvas paint. |
| 2026-07-11 | Deploy fix — **API + website now LIVE on Render** | **The Render deploy that failed the day before is fixed; the whole portal is now serving real data publicly.** Founder reported the `nepal-data-api` deploy failed. Local DB verified healthy first (`check-db` PG 17.6, not paused). Read the Render logs (never guessed) through three distinct failure modes: **(1)** `healthCheckPath` was `/v1/indicators` — a DB-backed route Render pings every ~10s; with bad credentials each ping failed auth and the repeated failures tripped Supabase's pooler `ECIRCUITBREAKER: too many authentication failures`, blocking even valid connections and marking the whole deploy failed. **Fix (code, committed 46eee58):** added a deliberately **DB-free `GET /health`** in `api/main.py` and pointed `healthCheckPath` at it (also changed in the Render dashboard settings, since editing `render.yaml` does NOT auto-update an existing service); +1 test (`test_health_is_db_free`), proven 200 even with a broken DSN. **(2)** After the health fix the deploy went Live but `/v1` still 500'd: `ECIRCUITBREAKER` again → the pooler username was plain `postgres`; the Supabase **session pooler requires `postgres.<project-ref>`** (`postgres.ptjqugriilhpxerwmuxx`). **(3)** Next error changed to `Network is unreachable` on an **IPv6** address `2406:da18:...` — the DSN was using the **direct** host `db.<ref>.supabase.co` (IPv6-only), which **Render free tier cannot reach**; must use the pooler host `aws-1-ap-southeast-1.pooler.supabase.com:5432`. Also confirmed the password must be URL-encoded (`@`→`%40`, `#`→`%23`). Verified the corrected pooler DSN **from my machine** before asking the founder to redeploy (read 55 indicators). Founder pasted the correct value → **Save, rebuild, and deploy**. **Now live & verified from the public internet:** `/health` 200; `/v1/indicators` 55; GDP_GROWTH 64 pts (WB, 2024=3.67); NRB_BFS_LENDING_RATE 59 pts (NRB, with `bfi_class` breakdowns); website `/` and `/banking` both 200. Lesson saved to memory (`render-supabase-pooler-deploy`). | Live curl: API `/health`=200, `/v1/indicators`=55, WB+NRB data endpoints return real series; web `/` & `/banking`=200. `make lint` clean; `make test` **35 passed** (34→+1). Local pooler-DSN connect read 55 indicators. | Founder-facing demo links now work: `nepal-data-api.onrender.com` + `nepal-data-web.onrender.com` (free tier sleeps ~15 min idle, first hit ~50s). Back to data work: extend BFS beyond C4 (C5–C7 levels, per-bank tables) + 2011–2021 backfill; Playwright smoke test for canvas paint. |
| 2026-07-10 | NRB-BFS load — **COMPLETE, live end to end** | **Founder restored `DATABASE_URL`; the full NRB BFS chain ran and the banking dashboard is live on real data.** Chain: `migrate` (0005 applied; 0001–0005 green) → `seed` (units 6, sources 2, datasets 2; NRB rows landed) → `seed-nrb` (35 indicators) → `seed-periods-ne` (**P2.S2 verified live at last**: fiscal years seeded + year sort keys realigned, idempotent) → `nrb-bfs-acquire` (**59/59 files** into the raw lake, sha256 manifest committed) → `nrb-bfs-extract` (**6,905 rows staged**, 59 consecutive BS months 2078-03…2083-01) → review: 2 random staged values re-verified programmatically against the original xlsx (Bhadau 2082 mobile-banking customers 28,240,106; Ashadh 2078 prepaid cards 65,786 — both exact) → `approve --all` (note on record) → `promote` (**quality gate PASSED; 6,905 loaded, 0 unchanged; release 8; ingestion_log success**). Warehouse now 7,930 observations (1,025 WB + 6,905 NRB), 59 month periods with true Gregorian dates + BS labels. **Verified via API + UI:** `/v1/data?indicator=NRB_BFS_LENDING_RATE` → 59 points with breakdowns; `/banking` data table renders all 59 months × 4 classes — first row Mid-Jul 2021 NPL 1.48/1.41/1.3/6.19, last row Mid-May 2026 5.6/5.41/6.13/12.19, all matching the source files. **Free-tier lessons (fixes committed):** (1) Supabase drops connections under sustained row-by-row writes → extract/promote now use one bulk change-aware SELECT + pipelined `executemany` batches, per-file short connections, one retry; (2) a crashed run leaves an 'idle in transaction' zombie via the pooler that blocks the next run into statement timeout → both pipelines now clear their own stale sessions pre-flight (safe: only our pipelines write these tables); (3) canvas paint remains unverifiable in the headless preview (P1 lesson e) — founder should eyeball localhost:3000/banking once. | check-db OK (PG 17.6); migrate-status 0001–0005 APPLIED; acquire 59/59 + manifest 59; extract 6,905; spot-checks 2/2 exact; promote 6,905/0 release 8; obs total 7,930, is_latest clean; API 59-point series; UI table 59×4 verified. | Eyeball the chart paint in a real browser; commit; extend BFS beyond C4 (C5–C7 levels, per-bank tables) and 2011–2021 backfill via the JS-viewer archive; Playwright smoke test for canvas (P1 lesson e). |
| 2026-07-10 | NRB-BFS onboarding (founder priority; code complete, DB load pending) | **NRB "Banking and Financial Statistics — Monthly" onboarded end to end in code: acquire → raw lake → parse → staging → review → promote → API breakdowns → banking dashboard.** Founder asked for NRB monthly statistics as time series + a dashboard, aggregates first. **Recon (evidence, never guessed):** all 59 published .xlsx (Ashadh 2078 / Jul 2021 → Baisakh 2083 / May 2026) downloaded and scanned; table **C4 "Major Financial Indicators" is structurally identical in every file** (older 14/18/20-table eras differ elsewhere, not in C4); exactly **35 distinct row labels** after normalization; label wobble is footnote markers/spacing only. **Trap documented:** "CCD Ratio" (abolished 2022) vs "CD Ratio" are different regulatory concepts — kept as separate indicators. **(a)** `ingestion/nrb/bfs_layout.py` — PURE layout module: 35-indicator REGISTRY, normalizer, BS-month romanization map, C4 matrix parser; validated against all 59 real files: 59/59 parsed, **6,905 values**, 59 consecutive BS months, 0 unmatched labels, 0 gaps. **(b)** `bfs_acquire.py` — listing scrape → new files into the raw lake (`payload.xlsx` via new optional `payload_filename` on `RawLake.store`) + committed manifest `reference/nrb/bfs_manifest.json` as idempotency ledger. **(c)** Migration `0005_nrb_bfs_staging.sql` — staging + review workflow (pending/approved/rejected/promoted; changed value on a promoted cell auto-resets to pending = revision re-review). **(d)** `bfs_extract.py` — lake/manifest (or `--from-dir`) → staging upsert; `--dry-run` needs no DB; unmatched labels fail the run (report, never guess). **(e)** `scripts/nrb_bfs.py` — `status` (with spot-check sample) / `approve` / `reject --note` / `promote`: creates BS-month `time_periods` from `bs_calendar` with true Gregorian dates (`bs_label` "Baisakh 2083", label "Mid-May 2026", sort_key per decision 0002), quality gate, one release, change-aware load with `breakdowns={"bfi_class": …}` (4 classes), status='provisional' (NRB's own wording), ingestion_log; staging rows marked promoted. **(f)** Seeds: NRB source (`central_bank`), BFS dataset (file/monthly), COUNT unit, `indicators_nrb.csv` (35, generated from REGISTRY; sync locked by test) + `scripts/seed_nrb.py` / `make seed-nrb`. **(g)** API: `observations.breakdowns` now exposed (`Observation.breakdowns`, default {} — WB responses unchanged). **(h)** Web: `/banking` dashboard — C4-section indicator picker, 4 bank-class lines (ECharts), Levels/MoM/YoY views, data table, NRB attribution; `npm run build` clean; dev-server smoke: page renders with §3.6 error state while API is down, 0 console errors. **(i)** openpyxl~=3.1 pinned. **Blocked/pending:** `.env` `DATABASE_URL` is EMPTY (found this session) — founder must paste the Supabase connection string; then: `make migrate` (0005) → `seed` → `seed-nrb` → `nrb-bfs-acquire` (59 files) → `extract` → review+`promote` → verify dashboard with real data + spot-check 2–3 values against NRB's published file, and update this log. Also committed as found (previous session's uncommitted work): P2.S2 fiscal-period seeding files + phase 3–5 step files — P2.S2 DB verification equally pending. | `make test` **34 passed** (18 → +16); `make lint` clean (ruff + mypy, 33 files); parser proof 59/59 files & 6,905 values & 0 unmatched; `npm run build` clean (route `/banking` 4.4 kB); dry-run extract over all 59 real files OK. | Founder pastes `DATABASE_URL` into `.env`; run the DB chain above; then extend BFS beyond C4 (deposits/loans levels C5–C7, per-bank tables) and backfill 2011–2021 via the JS-viewer archive (browser automation). |
| 2026-07-05 | P2.S1 (complete) | **BS↔AD day-level calendar reference + UTF-8 stdout helper — Phase 2 begins.** *(Session note: the Supabase dev project had auto-paused after ~3 weeks idle — `check-db` failed with the pooler's `tenant/user not found`; founder resumed it from the dashboard, a full restore — schema 0001–0003 and all 1025 obs / 71 periods intact — before this step ran.)* **(a)** `ingestion/common/io_utf8.py` — `configure_stdout_utf8()` reconfigures stdout/stderr to UTF-8 (carry-forward Lesson 1: Windows console cp1252 crashes on Devanagari); verified it prints `काठमाडौं / नेपाल राष्ट्र बैंक` and reports `utf-8`. Runbook now states every entrypoint must call it. **(b)** Migration `0004_bs_calendar.sql` — `bs_calendar(bs_year, bs_month, bs_day, gregorian_date, weekday)`, PK on the BS date + **UNIQUE(gregorian_date)** so the map is a strict bijection. **(c)** Data sourced from `opensource-nepal/node-nepali-datetime` `constants.ts` (commit `feea859`, **GPL-3.0** — note: we use only the non-copyrightable month-length *facts* from the Nepal Rajpatra; raw file + extracted `bs_month_lengths.json` + `PROVENANCE.md` under `reference/calendar/`). Anchor **BS 2000-01-01 = AD 1943-04-14**. `scripts/load_bs_calendar.py` (`make load-calendar`) walks the month-length table day by day; idempotent COPY-staged upsert. **Range decision:** loaded **BS 2000–2099** (AD 1943–2043), not the step's suggested 1970 — BS 2000 is where gazetted data begins and our data era starts ≈BS 2017; padding earlier would guess mappings (Prime Directives 7 + "never guess"). **Verified:** 36,525 rows; perfect bijection (rows = distinct AD dates = span days); idempotent re-run (`inserted=0 updated=0`); **two independent converters** agree on BS 2080-01-01 = Fri 2023-04-14 (hamropatro + nepalicalendar) and the fiscal boundary Shrawan 1 2080 = Mon 2023-07-17. Offline `tests/test_bs_calendar.py` locks the anchor, both dates, and the bijection. | `make migrate-status` 0004 APPLIED; loader 36,525 rows + idempotent; 2 converters agree on 3 dates incl. FY boundary; `make lint` clean; `make test` 18 passed (15 → +3); README setup updated with `make load-calendar`. | **P2.S2 — fiscal-year & BS time periods:** seed `period_type='fiscal_year'` rows into `time_periods` from `bs_calendar` (Shrawan-1 → next Ashadh-end Gregorian dates, `bs_label` '2080/81', chronological `sort_key` interleaving with the existing calendar years), idempotent `make seed-periods-ne`. |
| 2026-06-14 | **P1.S12 — PHASE 1 COMPLETE** | **Walking skeleton verified end to end; Phase 1 closed.** (1) **Rebuild drill** — followed the README's documented commands on the real toolchain: `make setup` → `make migrate` (0001–0003 already applied, nothing to do) → `make seed` (idempotent: 20 indicators / 71 periods / 1 geography, all WDI codes re-verified live) → `make ingest-wb` (idempotent: 0 loaded, 1025 unchanged, quality gate PASSED, new release id 6) → `make api` + `make web` (both serving 200). **README gap found & fixed:** the "First-time setup" never told a newcomer to `cp .env.example .env` and fill it in — without `DATABASE_URL` nothing downstream runs; added an explicit `.env` step with explanation. (2) **Traceability drill** (Quality Bar item 1) — `scripts/trace_demo.py` traces the chart's GDP-growth-2020 value **−2.37%** → observations row 212 (status final, is_latest) → release 3 → World Bank WDI (`NY.GDP.MKTP.KD.ZG`) → immutable raw-lake object `worldbank/wdi/NY.GDP.MKTP.KD.ZG/2026-06-13T154618Z/payload.json` → source URL, **in 1.57 s** (target: under a minute). The row still belongs to release 3 even after today's release-6 re-ingest — because the value was unchanged, which is exactly the is_latest/idempotency model proving itself. (3) Exit checklist all green. **Lessons captured for Phase 2 step file** (see below). | Documented chain runs idempotently; `make migrate-status` 0001–0003 APPLIED; trace chart→raw in 1.57 s; `make lint` clean (mypy 19 files); `make test` 15 passed; README rebuild gap fixed. | **Phase 2 — the Nepali layer.** In the Claude Project say: "Phase 1 exit criteria met — generate the Phase 2 step file." Phase 2: BS↔AD calendar bridge, old/new federal geography crosswalk, NRB Excel with staging-and-review, Census 2021. |
| 2026-06-14 | Phase 1 — lessons for Phase 2 | **(a) Windows console encoding is a recurring tripwire** — bit us on Devanagari display (P1.S4) and an em-dash mojibake in migrate output; Phase 2 is Devanagari-heavy (BS labels, Nepali names, Nepali source files), so every script that prints must force UTF-8 stdout (`PYTHONUTF8=1`) — make it a Phase 2 standard/helper, not a per-script fix. **(b) The is_latest + release model is proven** under real re-ingestion (unchanged values keep the prior release's row latest); NRB revisions in Phase 2 can rely on it. **(c) Raw-lake paths are keyed by the source's own code** (WDI code), not our internal indicator code — tracing maps through `source_concept`; document this path convention in the source-onboarding runbook so NRB/census follow a consistent scheme. **(d) `.env` creation is the #1 fresh-clone tripwire** — now documented; keep `.env.example` authoritative as Phase 2 adds source keys. **(e) Frontend canvas rendering can't be verified by automation here** (no headless browser) — Phase 2/3 frontend work should add a lightweight Playwright smoke test so chart rendering is provable, not just human-checked. **(f) Two-calendar reality starts now** — Phase 1 used calendar years only; `time_periods` already carries `period_type`/`bs_label`, so the BS↔AD bridge is the first real test of that dimension design. | — | (folded into the Phase 2 step-file prompt) |
| 2026-06-14 | P1.S11 (complete) | **First interactive chart in the browser — the slice is now end-to-end.** Built the Next.js 14 app in `web/` (App Router, TypeScript strict): `lib/api.ts` (typed client mirroring `api/models.py`, custom `ApiError` separating network-down from HTTP errors), `components/IndicatorChart.tsx` (ECharts line chart via `echarts-for-react`, dynamically imported with `ssr:false` so canvas never renders server-side; hover tooltip shows year + exact value + unit; unit-aware formatting — `%`, `US$`, compact axis for currency), `app/page.tsx` (client page: indicator dropdown grouped by topic via `<optgroup>`, defaults to GDP_GROWTH; loading/empty/error states; **§3.6 error state** — says what happened and offers the data as a table; "View data behind this chart" table toggle as the accessible text alternative; attribution line "Source: World Bank — World Development Indicators, release {date} · License: CC BY 4.0" linking to data.worldbank.org), `globals.css` (minimal clean styling — real design system is Phase 3). Added `make web-setup` (npm install) and `make web` (next dev) to the Makefile; README "Seeing the portal" run instructions. The browser talks **only** to the API, never the DB (Master Prompt §3.5); CORS already allowed localhost:3000 from P1.S10. **Verified:** `npm run build` compiles clean (strict TS, statically prerendered, 434 kB first load); dev server serves `/` → 200 with title; API returns `Access-Control-Allow-Origin: http://localhost:3000` for the browser origin; the milestone series GDP_GROWTH renders 2018=7.62 → 2019=6.66 → **2020=−2.37 (COVID dip)** → 2021=4.84 with World Bank/WDI/CC BY 4.0 provenance; dropdown lists 20 indicators across 6 topics. **Note:** no headless browser in this environment, so the canvas paint itself is unverified by automation — founder should open localhost:3000 and confirm the chart draws and hover works (one human check). `npm audit` reports 2 transitive dev-dep advisories — non-blocking for local skeleton, revisit before deploy. | `npm run build` clean; `/`→200 + title; CORS header correct for localhost:3000; live API data flow confirmed (GDP dip + provenance); 20 indicators/6 topics. | **P1.S12 — phase close:** rebuild drill from README alone, traceability drill (chart number → raw source in <1 min), confirm `make test`/`make lint` green and 0001–0003 applied, then close Phase 1 and note lessons for the Phase 2 step file. |
| 2026-06-13 | P1.S10 (complete) | **Minimal read-only API (FastAPI).** `api/` package: `repository.py` (Repository Protocol + PostgresRepository + row dataclasses — read-only), `models.py` (Pydantic), `main.py` (app + routes). Endpoints under `/v1/`: `GET /v1/indicators`, `GET /v1/indicators/{code}`, `GET /v1/data?indicator=&geo=` — every data response wrapped with a provenance block (source, dataset, license, release date, unit). CORS allows localhost:3000 (for P1.S11). Repository injected via `Depends` so tests use a `FakeRepository` offline (Annotated style, no B008). Added deps fastapi/uvicorn[standard]/httpx; `make api`. 4 API tests. **Verified live vs DB:** /v1/indicators=20; /v1/data GDP_GROWTH=64 obs, provenance World Bank/CC BY 4.0, 2020=−2.37; unknown code→clean 404; uvicorn boots (HTTP 200 /openapi.json). | `make test` 15 passed; `make lint` green; live curl + TestClient evidence; server boot 200. | **P1.S11 — first interactive chart (Next.js)** — the founder-facing milestone; best done with the founder present. |
| 2026-06-13 | P1.S9 (complete) | **In-pipeline data-quality gate.** `ingestion/common/quality.py` (`Candidate`, `run_quality_gate`): (a) growth rates ±50, share/level percentages [-10,200] (upper bound allows gross-enrolment >100, rejects e.g. 250% literacy); (b) population strictly positive within [1e6,1e8]; (c) FK-resolution asserted; (d) per-indicator year-gap continuity as INFO (not failure); (e) finite-number check. Each rule documented with rationale. Wired into pipeline **before** release finalize — failure marks ingestion_log `failed`, writes nothing, leaves is_latest untouched. 6 new tests. **Proved both ways:** injected literacy=250% → BLOCKED, log `failed` with readable reason, observations stayed 1025; normal `make ingest-wb` → quality gate PASSED (continuity INFO shown), idempotent. | `make test` 11 passed; `make lint` green; blocked-run demo (obs before=after=1025, log failed); normal run PASSED. | **P1.S10 — minimal public API** (done same session). |
| 2026-06-13 | P1.S8 (complete) | **World Bank WDI ingestion pipeline — real data loaded.** `ingestion/worldbank/pipeline.py` + `make ingest-wb`: fetches all 20 indicators for Nepal (NPL) from WB API v2 (paged), stores each raw response in the lake BEFORE parsing, creates one `releases` row per run, parses year→time_period / NP→geography / value→numeric (nulls skipped+counted), loads under the release with `ingestion_log`. **Change-aware loading:** only new/changed values are inserted (unchanged skipped), so re-runs keep row count stable; real revisions insert a new row and the is_latest trigger demotes the old. Offline parse test via `tests/fixtures/wb_sample.json`. **Run 1:** 20 indicators, 1320 points, 1025 loaded, 295 null-rejected, 20 raw payloads archived. **Verified vs live API:** GDP_GROWTH 2020=−2.3696 (COVID), POP_TOTAL 2021=29,475,010, CPI_YOY 2022=7.6702 — all exact matches (no year off-by-one). **Run 2 (idempotency):** 0 loaded, 1025 unchanged, new release row; total observations stayed 1025, is_latest=1025, 0 cells with duplicate latest. | `make ingest-wb` summaries; DB cross-check ALL MATCH=True; ingestion_log success(1320/1025/295); `make lint`/`make test` (5) green. | **P1.S9 — data-quality gate:** in-pipeline checks (ranges, FK resolution, continuity); block + log bad data before promotion. |
| 2026-06-13 | P1.S7 (complete) | **Raw data lake.** Wrote `ingestion/common/raw_lake.py`: `RawLake` with two backends — `LocalFilesystemBackend` (tests, offline) and `SupabaseStorageBackend` (REST API). `store()` computes SHA-256, writes `<dataset>/<utc-stamp>/payload.json` + `metadata.json` sidecar (hash, fetched_at, source_url, size), never overwrites (colon-free timestamps; put refuses existing). Added `pythonpath=["."]` to pytest so project packages import; `ingestion/` + `ingestion/common/` made packages. Created private Supabase bucket `raw-lake` via API; live smoke test stored→read-back byte-for-byte (hash verified)→deleted. Storage keys added to `.env` by founder (`SUPABASE_URL`, `SUPABASE_SERVICE_KEY`, `STORAGE_BUCKET`). **Lessons:** (1) founder first pasted the JWT *signing secret* (88 chars) + a URL with `/rest/v1/` suffix — fixed by using an `sb_secret_…` key and stripping the URL to base host; (2) our code accepts both `sb_secret_…` and legacy `eyJ…` service_role keys. | `make test` 4 passed (incl. offline raw-lake test); `make lint` green; smoke test read-back match True, cleanup confirmed; `git grep` for keys empty; `.env` untracked. | **P1.S8 — World Bank ingestion pipeline:** fetch 20 indicators for Nepal, raw-first, parse, load under a release, idempotent, with ingestion_log. The big one. |
| 2026-06-13 | P1.S6 (complete) | **Reference data seeded.** Created CSV seeds in `db/seeds/` (units, sources, datasets, geographies, indicators) + `scripts/seed.py` + `make seed` — idempotent via ON CONFLICT upserts on natural keys. Loaded: 5 units, World Bank source + WDI dataset (access_method=api), Nepal geography (code NP, नेपाल), 71 calendar-year periods (1960–2030), and **20 indicators with name_en/definition_en fetched from the World Bank metadata API**; each WDI code verified live (Prime Directive 7 — failures reported, never guessed); name_ne left NULL (TODO Phase 3 translation). Also wrote **runbook `docs/runbooks/adding-a-data-source.md`** — the repeatable source-onboarding checklist (canonical-model + adapter-per-source; raw→staging→warehouse). | `make seed` → counts units=5/sources=1/datasets=1/geographies=1/time_periods=71/indicators=20; ran twice, counts unchanged (idempotent); all 20 codes verified; `make lint`/`make test` green. | **P1.S7 — raw data lake:** private Supabase Storage bucket `raw-lake` + `ingestion/common/raw_lake.py` (hash + timestamp + source URL, immutable writes). Needs a storage key added to `.env`. |
| 2026-06-13 | P1.S5 (complete) | **Observations fact table (migration 0003) — the heart.** Created `observations` (FKs to indicators/geographies/time_periods/datasets/releases, all NOT NULL; `value numeric NOT NULL`; `unit_id` FK; `breakdowns jsonb DEFAULT '{}'`; `status` CHECK provisional/revised/final/estimated; `is_latest bool DEFAULT true`; footnote; timestamps). Uniqueness per Blueprint §4.2: UNIQUE(indicator_id,geography_id,time_period_id,breakdowns,release_id). Two justified indexes (partial is_latest series index; release index). **Chose a DB trigger** for is_latest (over app code) so the invariant can't be bypassed — `observations_set_is_latest()` AFTER INSERT demotes older rows for the same cell. **Verified live:** duplicate-in-same-release rejected by unique constraint; same cell in a new release flips old row is_latest→false, new→true, old value retained; all test data rolled back (0 rows). Also added **Decision 0001** (docs/decisions): CSV+Excel Nepal-scoped downloads with a provenance sheet — built Phase 3 on the Phase 1 API. | `make migrate-status` → 0001–0003 APPLIED; duplicate REJECTED + is_latest revision proven; `make lint`/`make test` green. | **P1.S6 — seed reference data:** Nepal geography, year periods 1960–2030, units, World Bank source/dataset, 20 WDI indicators (verify each code against live API). Good moment to fold in founder's own data shape if sharing a sample. |
| 2026-06-13 | P1.S4 (complete) | **Dimension tables (migration 0002).** Created **units, indicators, geographies, time_periods** per Blueprint §4.2: indicators with permanent unique `code`, NOT NULL `unit_id` FK, `topic` CHECK (8 blueprint topics), `source_concept`, nullable `preferred_source_id`; geographies with unique `code`, `level` CHECK (country/province/district/local_unit/old_region/old_district), self-FK `parent_id`, validity dates; time_periods with `period_type` CHECK, gregorian_start/end, bs_label/gregorian_label/sort_key, UNIQUE(period_type,gregorian_start,gregorian_end). `set_updated_at` trigger on all four. **Devanagari smoke test passed** — wrote/read `नेपाल` (exact code-point match), row cleaned up. **Lesson (Windows):** printing Devanagari to the console needs `PYTHONUTF8=1`/`PYTHONIOENCODING=utf-8` (cp1252 default raises UnicodeEncodeError) — purely a console-display issue, DB storage is correct UTF-8; future pipelines must set UTF-8 stdout. | `make migrate-status` → 0001+0002 APPLIED; DB lists units/indicators/geographies/time_periods; Devanagari round-trip `True`. `make lint`/`make test` green. | **P1.S5 — observations fact table (migration 0003):** the big table; uniqueness constraint + is_latest revision mechanics. |
| 2026-06-13 | P1.S3 (complete) | **Migration tooling + provenance spine.** Added `scripts/migrate.py` (yoyo wrapper: apply/list/rollback, loads .env) and `make migrate` / `make migrate-status` / `make migrate-rollback`. Added `psycopg2-binary` as yoyo's driver (app code keeps psycopg v3). Wrote migration `0001_provenance.sql` (+ `.rollback.sql`) creating **sources, datasets, releases, ingestion_log** per Blueprint §4.2 / Master Prompt §3.2 — snake_case, plural, `id` PKs, `<table>_id` FKs, NOT NULL defaults, CHECK enums (sources.type, datasets.access_method ∈ api/file/scrape/manual, ingestion_log.status ∈ running/success/failed), `raw_file_refs` jsonb, and a reusable `set_updated_at()` trigger on all four tables. Applied successfully; verified all four tables exist with correct column counts (9/10/8/13) alongside yoyo's bookkeeping tables. **Lesson:** yoyo SQL rollback convention = sibling `<name>.rollback.sql` file; verified it pairs as one migration via `migrate-status` before applying. | `make migrate-status` → `[APPLIED] 0001_provenance`; DB query lists sources/datasets/releases/ingestion_log. `make lint`/`make test` green. | **P1.S4 — dimension tables (migration 0002):** units, indicators, geographies, time_periods (Devanagari smoke test). |
| 2026-06-13 | P1.S2 (complete) | **Secure database connection established.** Created git-ignored `.env` with `DATABASE_URL` (Supabase **session pooler**, IPv4-friendly), `scripts/check_db.py` (loads .env, connects, prints version+time, exits non-zero on failure), and `make check-db`. Connection verified: **PostgreSQL 17.6**, server time returned. `.env` confirmed untracked + git-ignored — password stays local. **Lesson:** initial connects failed with "password authentication failed"; root cause was the Supabase password reset not taking effect (likely a too-short password silently rejected). Fixed by resetting to a longer alphanumeric password and watching for the green confirmation, then matching it in `.env`. Network/pooler were never the problem. | `make check-db` prints PostgreSQL 17.6; `git status` excludes `.env`. | **P1.S3 — migration tooling + provenance tables** (yoyo-migrations; migration `0001_provenance.sql`: sources, datasets, releases, ingestion_log). |
| 2026-06-13 | P1.S1 (complete) | **Local development environment built.** Node.js v24 already present; installed Python 3.12.10 (via winget, user scope) and GNU make (ezwinports). Created `pyproject.toml` (pinned deps: requests, psycopg, python-dotenv, yoyo-migrations; dev: pytest, ruff, mypy; + ruff/mypy/pytest config), `Makefile` (targets: setup, test, lint, fmt — OS-aware venv paths), and `.venv`. Added first test `tests/test_sanity.py` (Python-version + dependency-import checks) so the toolchain has real targets. `make lint` ✅ green, `make test` ✅ 2 passed. README "How to run" updated with prerequisites and commands. **Note for rebuild drill (P1.S12):** Windows founder — `make` is not built in; README documents the one-time `winget install ezwinports.make`. | Commit `P1.S1: development environment and tooling`; `make lint`/`make test` both exit 0. | **P1.S2 — secure database connection** (`.env` with DATABASE_URL from Supabase, `scripts/check_db.py`, `make check-db`). |
| 2026-06-13 | P0.S5 — **PHASE 0 COMPLETE** | Claude Project "Nepal Data Portal" created in the Claude app; blueprint, master prompt, and phase-0 step file added to project knowledge; custom instructions set per P0.S5 action 3. All Phase 0 exit criteria met: GitHub secured with 2FA, Supabase dev project `nepal-data-portal-dev` active (Singapore), Claude Code installed and working, private repo with §3.1 skeleton pushed, Claude Project configured. | Claude Project answers "Status check" with current phase/step; Supabase + GitHub both live. | **P1.S1 — local development environment** (install Python 3.12+, Node.js LTS, project tooling). See docs/steps/phase-1-steps.md. |
| 2026-06-13 | P0.S2 (complete) | **Supabase set up.** Organization `Indra Giri` created on the Free plan; development project `nepal-data-portal-dev` created and active. Region: **Singapore**. Data API settings: Data API ON, automatically-expose-new-tables OFF, automatic RLS ON. Database password generated and stored in password manager (never in repo/chat). Table Editor confirmed empty — correct; tables come only via migrations from Phase 1. | Supabase dashboard shows `nepal-data-portal-dev` active in Singapore. | P0.S5 — configure the "Nepal Data Portal" Claude Project (manual, in the Claude app). Then Phase 0 is closed and P1.S1 begins. |
| 2026-06-11 | P0.S4–P0.S5 (partial) | **Phase 0 partially complete.** Done: GitHub account active with 2FA; gh CLI installed; Claude Code (Desktop) installed and tested; private repo `Nepal_data_base_project` created with full §3.1 skeleton, founding documents, README, PROJECT_LOG, .gitignore, .env.example — all pushed to GitHub. **Remaining before Phase 1:** (1) P0.S2 — Supabase account and `nepal-data-portal-dev` project not yet created; (2) P0.S5 — Claude Project "Nepal Data Portal" not yet configured in the Claude app (manual step: add blueprint, master prompt, and phase-0-steps to project knowledge; set session custom instructions per P0.S5 action 3). | Repo at github.com/indragiri-ai/Nepal_data_base_project — 2 commits, structure verified. | Complete P0.S2 (Supabase), then P0.S5 (Claude Project config). Once both are done, proceed to P1.S1. |
